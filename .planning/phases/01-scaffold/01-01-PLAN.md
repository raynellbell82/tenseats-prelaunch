---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - components.json
  - app/globals.css
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - SCAF-02
  - SCAF-03
  - SCAF-04
  - SCAF-05
  - SCAF-11

must_haves:
  truths:
    - "Next.js project exists with correct directory structure"
    - "All config files match source repo versions"
    - "npm install completes without errors"
    - "package.json has correct name and dev script port"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "tenseats-prelaunch"
    - path: "next.config.ts"
      provides: "Next.js config with image domains and standalone output"
      contains: "standalone"
    - path: "tsconfig.json"
      provides: "TypeScript configuration from source"
    - path: "postcss.config.mjs"
      provides: "PostCSS config from source"
    - path: "components.json"
      provides: "shadcn/ui config from source"
    - path: "app/globals.css"
      provides: "Global CSS from source"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "node_modules/.package-lock.json"
---

<objective>
Initialize the Next.js project, copy config files from source, create standalone next.config.ts, derive package.json, and install dependencies.

Purpose: Establish the project skeleton with all configuration and dependencies so source files can be copied in the next plan.
Output: Working Next.js project root with node_modules installed and all config files in place.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Init Next.js project and replace boilerplate with source configs</name>
  <files>package.json, tsconfig.json, postcss.config.mjs, components.json, app/globals.css, app/layout.tsx, app/page.tsx</files>
  <action>
1. Run `npx create-next-app@latest . --ts --tailwind --eslint --app --no-src-dir --skip-install --disable-git --use-npm --import-alias "@/*"` from the project root `/Users/tenseats/Documents/dev/tenseats-prelaunch`. This will scaffold into the current directory. If it complains the directory is not empty (due to `.planning/` and prompt file), move those temporarily, run create-next-app, then move them back.

2. Delete ALL create-next-app boilerplate content: remove `app/page.tsx` content (will be replaced later in Phase 3), remove `app/layout.tsx` default content (will be replaced in Phase 2), remove any `app/favicon.ico` or `public/` boilerplate files that create-next-app generates.

3. Copy these config files from source repo `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/`:
   - `tsconfig.json` (overwrite the generated one)
   - `postcss.config.mjs` (overwrite the generated one)
   - `components.json` (copy as-is)
   - `app/globals.css` (overwrite the generated one)

4. Keep `app/layout.tsx` as a minimal placeholder (just renders children with html/body tags) and `app/page.tsx` as a minimal placeholder (just renders a div with "Tenseats Pre-Launch" text). These will be properly built in Phase 2/3.

IMPORTANT: Do NOT copy `tailwind.config.ts` — the project uses Tailwind v4 with `@import "tailwindcss"` syntax, no config file needed.
  </action>
  <verify>
    <automated>test -f tsconfig.json && test -f postcss.config.mjs && test -f components.json && test -f app/globals.css && test -f app/layout.tsx && test -f app/page.tsx && echo "All config files present"</automated>
  </verify>
  <done>All create-next-app boilerplate removed. Source config files (tsconfig, postcss, components.json, globals.css) in place. Minimal placeholder layout and page exist.</done>
</task>

<task type="auto">
  <name>Task 2: Derive package.json from source and install dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
1. Read the source repo's `package.json` at `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/package.json`.

2. Copy it entirely to the project root, then make ONLY these two changes:
   - `"name"`: change from `"tenseats-marketplace-platform"` to `"tenseats-prelaunch"`
   - `"scripts.dev"`: change to `"next dev --turbopack --hostname 0.0.0.0 -p 3001"` (add `-p 3001` to avoid conflict with main app on port 3000)
   - Keep ALL other fields identical — same dependencies, devDependencies, versions, scripts (build, start, lint, etc.)

3. Run `npm install` from the project root. This must complete without errors. All ~90 dependencies must install with exact version parity to the source.

IMPORTANT: Do not manually construct the package.json. Copy from source and modify only the two fields specified above. This ensures exact version parity per the project decision.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && node -e "const p=require('./package.json'); if(p.name!=='tenseats-prelaunch') throw 'wrong name'; if(!p.scripts.dev.includes('3001')) throw 'wrong port'; console.log('package.json OK')" && test -d node_modules && echo "node_modules exists"</automated>
  </verify>
  <done>package.json derived from source with only name and dev port changed. npm install completed successfully. node_modules populated with all dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Create standalone next.config.ts</name>
  <files>next.config.ts</files>
  <action>
Create `next.config.ts` (do NOT copy from source — this is a new standalone config). Contents:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "api.tenseats.io",
      },
      {
        protocol: "https",
        hostname: "images.unsplash.com",
      },
    ],
  },
};

export default nextConfig;
```

This config:
- Sets `output: "standalone"` for Coolify Docker deployment
- Allows images from `api.tenseats.io` (Convex file storage) and `images.unsplash.com`
- Does NOT include any source-repo-specific config (middleware matchers, rewrites, etc.)
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && node -e "const fs=require('fs'); const c=fs.readFileSync('next.config.ts','utf8'); if(!c.includes('standalone')) throw 'missing standalone'; if(!c.includes('api.tenseats.io')) throw 'missing convex domain'; if(!c.includes('unsplash')) throw 'missing unsplash'; console.log('next.config.ts OK')"</automated>
  </verify>
  <done>next.config.ts created with standalone output mode and image domains for api.tenseats.io and unsplash.</done>
</task>

</tasks>

<verification>
All config files present and correct. npm install completed. next.config.ts is standalone (not copied from source).
</verification>

<success_criteria>
- tsconfig.json, postcss.config.mjs, components.json, globals.css copied from source
- package.json has name "tenseats-prelaunch" and dev script with port 3001
- node_modules installed successfully
- next.config.ts has standalone output and correct image domains
- No create-next-app boilerplate remains
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
