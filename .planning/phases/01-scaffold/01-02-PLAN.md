---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components/ui/
  - components/launch/
  - components/join/
  - components/auth/
  - components/shared/footer/
  - lib/auth-client.ts
  - lib/convex.tsx
  - lib/utils.ts
  - lib/auth-server.ts
  - convex/schema.ts
  - convex/auth.config.ts
  - convex/auth.ts
  - convex/authHelpers.ts
  - convex/launch/
  - convex/metros.ts
  - convex/platformSettings.ts
  - convex/emailTemplates.ts
  - convex/webhookSigning.ts
  - convex/convex.config.ts
  - convex/tsconfig.json
  - convex.json
  - app/api/auth/[...all]/route.ts
autonomous: true
requirements:
  - SCAF-01
  - SCAF-06
  - SCAF-07
  - SCAF-08
  - SCAF-09
  - SCAF-10
  - SCAF-14

must_haves:
  truths:
    - "All required component directories exist with source files"
    - "All required lib files exist"
    - "Full Convex schema.ts is present unchanged from source"
    - "convex/http.ts does NOT exist in the project"
    - "Auth API catch-all route exists"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Full shared schema unchanged from source"
    - path: "convex.json"
      provides: "Convex functions config with better-auth external package"
    - path: "lib/convex.tsx"
      provides: "Convex client provider"
    - path: "lib/auth-client.ts"
      provides: "Better Auth client config"
    - path: "app/api/auth/[...all]/route.ts"
      provides: "Auth API catch-all route"
    - path: "components/ui/"
      provides: "shadcn/ui component library"
    - path: "components/launch/"
      provides: "Launch flow components"
    - path: "components/join/"
      provides: "Join page components"
  key_links:
    - from: "lib/convex.tsx"
      to: "convex/_generated"
      via: "import from convex/_generated"
      pattern: "convex/_generated"
    - from: "app/api/auth/[...all]/route.ts"
      to: "lib/auth-server.ts"
      via: "import"
      pattern: "auth-server"
---

<objective>
Copy all source component, lib, and Convex files from the main Tenseats repo into the pre-launch project.

Purpose: Populate the project with all the application code needed for the pre-launch site, maintaining exact copies from source.
Output: All component directories, lib files, Convex files, and auth route in place.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy component and lib directories from source</name>
  <files>components/ui/, components/launch/, components/join/, components/auth/, components/shared/footer/, lib/auth-client.ts, lib/convex.tsx, lib/utils.ts, lib/auth-server.ts</files>
  <action>
Source repo: `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform`
Target: `/Users/tenseats/Documents/dev/tenseats-prelaunch`

1. Copy component directories (use `cp -R` to preserve structure):
   - `components/ui/` (entire directory — shadcn/ui components)
   - `components/launch/` (entire directory — launch flow components)
   - `components/join/` (entire directory — join page components)
   - `components/auth/` (entire directory — auth components)
   - `components/shared/footer/` (footer component — create `components/shared/` first, then copy `footer/` subdirectory)

2. Copy lib files (individual files, not the whole directory):
   - `lib/auth-client.ts`
   - `lib/convex.tsx`
   - `lib/utils.ts`
   - `lib/auth-server.ts`

Create the `components/` and `lib/` directories first if they don't exist.

IMPORTANT: Copy these files as-is. Do NOT modify any source files. The main app repo is read-only.
IMPORTANT: Do NOT copy other component directories (admin, dashboard, feed, etc.) — only the 5 listed above.
IMPORTANT: Do NOT copy other lib files (admin-nav.ts, features.ts, hooks/, etc.) — only the 4 listed above.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && test -d components/ui && test -d components/launch && test -d components/join && test -d components/auth && test -d components/shared/footer && test -f lib/auth-client.ts && test -f lib/convex.tsx && test -f lib/utils.ts && test -f lib/auth-server.ts && echo "All component and lib files present"</automated>
  </verify>
  <done>All 5 component directories and 4 lib files copied from source. No modifications made to source repo.</done>
</task>

<task type="auto">
  <name>Task 2: Copy Convex files, convex.json, and auth API route</name>
  <files>convex/schema.ts, convex/auth.config.ts, convex/auth.ts, convex/authHelpers.ts, convex/launch/, convex/metros.ts, convex/platformSettings.ts, convex/emailTemplates.ts, convex/webhookSigning.ts, convex/convex.config.ts, convex/tsconfig.json, convex.json, app/api/auth/[...all]/route.ts</files>
  <action>
Source repo: `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform`
Target: `/Users/tenseats/Documents/dev/tenseats-prelaunch`

1. Create `convex/` directory if it doesn't exist.

2. Copy these Convex files (individual files):
   - `convex/schema.ts` (FULL file, unchanged — critical for shared deployment)
   - `convex/auth.config.ts`
   - `convex/auth.ts`
   - `convex/authHelpers.ts`
   - `convex/metros.ts`
   - `convex/platformSettings.ts`
   - `convex/emailTemplates.ts`
   - `convex/webhookSigning.ts`
   - `convex/convex.config.ts`
   - `convex/tsconfig.json`

3. Copy the launch directory: `convex/launch/` (entire directory with `cp -R`)

4. Copy `convex.json` from source root to project root (this configures Convex functions with better-auth as external package).

5. Create the auth API route directory and copy the route file:
   - Create `app/api/auth/[...all]/`
   - Copy `app/api/auth/[...all]/route.ts` from source

CRITICAL: Do NOT copy `convex/http.ts` — the project relies on the existing shared HTTP router. Verify after copying that `convex/http.ts` does NOT exist.

IMPORTANT: `convex/schema.ts` must be copied UNCHANGED. Any modification breaks the shared Convex deployment. Do not edit it.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && test -f convex/schema.ts && test -f convex/auth.config.ts && test -f convex/auth.ts && test -f convex/metros.ts && test -f convex/platformSettings.ts && test -d convex/launch && test -f convex.json && test -f "app/api/auth/[...all]/route.ts" && test ! -f convex/http.ts && echo "All Convex files present, http.ts absent"</automated>
  </verify>
  <done>All required Convex files copied. convex.json in place. Auth API route created. convex/http.ts confirmed absent.</done>
</task>

</tasks>

<verification>
All source files copied to correct locations. No extra directories copied. convex/http.ts does not exist. convex/schema.ts is byte-identical to source.
</verification>

<success_criteria>
- 5 component directories present (ui, launch, join, auth, shared/footer)
- 4 lib files present (auth-client.ts, convex.tsx, utils.ts, auth-server.ts)
- All 10 Convex files + launch directory present
- convex.json in project root
- app/api/auth/[...all]/route.ts exists
- convex/http.ts does NOT exist
- convex/schema.ts is unchanged from source (diff shows no changes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-02-SUMMARY.md`
</output>
