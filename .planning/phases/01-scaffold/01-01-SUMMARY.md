---
phase: 01-scaffold
plan: 01
subsystem: infra
tags: [nextjs, typescript, tailwindcss, shadcn, postcss, npm]

# Dependency graph
requires: []
provides:
  - "Next.js project skeleton with app router, TypeScript, Tailwind v4"
  - "All config files (tsconfig.json, postcss.config.mjs, components.json, globals.css) matching source repo"
  - "package.json derived from source with exact ~90-dependency version parity"
  - "node_modules installed with all dependencies"
  - "next.config.ts with standalone output and image domains for api.tenseats.io and unsplash"
affects: [02-convex, 03-auth, 04-ui-components, 05-homepage, 06-join, 07-why, 08-cities, 09-launch]

# Tech tracking
tech-stack:
  added:
    - "Next.js ^16.1.3 (app router, TypeScript, Tailwind v4)"
    - "convex ^1.31.5"
    - "better-auth ^1.4.18"
    - "tailwindcss ^4.1.18 (via @import tailwindcss, no config file)"
    - "tw-animate-css ^1.4.0"
    - "shadcn/ui (new-york style, lucide icons)"
    - "framer-motion ^12.34.0"
    - "stripe ^20.2.0"
    - "All ~90 deps from source repo at exact versions"
  patterns:
    - "Tailwind v4: @import tailwindcss syntax, no tailwind.config.ts"
    - "Standalone Next.js output for Coolify Docker deployment"
    - "legacy-peer-deps=true via .npmrc for peer dependency resolution"

key-files:
  created:
    - "package.json — derived from source, name=tenseats-prelaunch, dev port=3001"
    - "package-lock.json — lockfile from npm install"
    - "next.config.ts — standalone output, api.tenseats.io + unsplash image domains"
    - "components.json — shadcn/ui new-york config from source"
    - ".npmrc — legacy-peer-deps=true"
  modified:
    - "tsconfig.json — replaced boilerplate with source repo version"
    - "postcss.config.mjs — matches source (@tailwindcss/postcss plugin)"
    - "app/globals.css — replaced boilerplate with source CSS vars, dark theme, animations"
    - "app/layout.tsx — minimal placeholder (html/body, no Geist fonts)"
    - "app/page.tsx — minimal placeholder (Tenseats Pre-Launch div)"

key-decisions:
  - "Used legacy-peer-deps=true (.npmrc) to resolve @convex-dev/workpool peer conflict"
  - "next.config.ts is standalone (not copied from source) — includes output:standalone and image domains"
  - "Removed all create-next-app boilerplate SVGs from public/"
  - "No tailwind.config.ts — Tailwind v4 uses @import tailwindcss syntax"

patterns-established:
  - "Config parity: All config files except next.config.ts match source repo exactly"
  - "Port separation: Dev server on 3001, main app stays on 3000"

requirements-completed: [SCAF-02, SCAF-03, SCAF-04, SCAF-05, SCAF-11]

# Metrics
duration: 6min
completed: 2026-03-13
---

# Phase 1 Plan 01: Next.js Project Scaffold Summary

**Next.js 16 + Tailwind v4 project scaffolded with full source-repo config parity, ~90 dependencies installed, and standalone next.config.ts for Coolify Docker deployment**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-13T01:01:18Z
- **Completed:** 2026-03-13T01:07:34Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments
- Next.js project initialized via create-next-app, all boilerplate replaced with source configs
- All ~90 dependencies installed with exact version parity via derived package.json
- Standalone next.config.ts created with image domains for api.tenseats.io and images.unsplash.com

## Task Commits

Each task was committed atomically:

1. **Task 1: Init Next.js project and replace boilerplate with source configs** - `20de635` (feat)
2. **Task 2: Derive package.json from source and install dependencies** - `f4eb463` (feat)
3. **Task 3: Create standalone next.config.ts** - `0172259` (feat)

## Files Created/Modified
- `package.json` - Derived from source, name=tenseats-prelaunch, dev on port 3001
- `package-lock.json` - Generated lockfile from npm install (1048 packages)
- `.npmrc` - legacy-peer-deps=true (resolves workpool peer conflict)
- `next.config.ts` - Standalone output, image domains for Convex and Unsplash
- `components.json` - shadcn/ui new-york style config from source repo
- `tsconfig.json` - Source repo version (ES2017, bundler module resolution)
- `postcss.config.mjs` - Source version (@tailwindcss/postcss plugin)
- `app/globals.css` - Full source CSS variables, dark theme, animations, utilities
- `app/layout.tsx` - Minimal placeholder (html/body tags, metadata)
- `app/page.tsx` - Minimal placeholder (Tenseats Pre-Launch div)
- `.gitignore` - Generated by create-next-app

## Decisions Made
- Added `.npmrc` with `legacy-peer-deps=true` after npm install failed due to `@convex-dev/workpool` peer conflict with `@convex-dev/workflow`. Source repo also had workpool@0.2.19 installed, confirming legacy-peer-deps was the correct resolution.
- Did NOT copy `next.config.ts` from source — created standalone config per plan spec with only `output: standalone` and image domains specific to this deployment.
- No `tailwind.config.ts` created — Tailwind v4 uses `@import "tailwindcss"` in globals.css, no config file needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added .npmrc with legacy-peer-deps to resolve npm install peer conflict**
- **Found during:** Task 2 (npm install)
- **Issue:** `npm install` failed with ERESOLVE — `@convex-dev/workflow@0.3.6` requires `@convex-dev/workpool@^0.3.0` but package.json specifies `^0.2.19` (exact match to source repo)
- **Fix:** Ran `npm install --legacy-peer-deps` and created `.npmrc` with `legacy-peer-deps=true` so future installs work automatically
- **Files modified:** `.npmrc`
- **Verification:** npm install completed with 1048 packages; source node_modules also has workpool@0.2.19 confirming this is expected
- **Committed in:** `f4eb463` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - blocking peer dependency conflict)
**Impact on plan:** Auto-fix was necessary for npm install to complete. No scope creep. Source repo behavior is identical.

## Issues Encountered
- npm peer dependency conflict between `@convex-dev/workflow` and `@convex-dev/workpool` — resolved with legacy-peer-deps (same as source repo).

## User Setup Required
None - no external service configuration required for this plan.

## Next Phase Readiness
- Project skeleton complete with all config files and dependencies installed
- Ready for Phase 1 Plan 02: Convex setup (schema copy, convex/ directory)
- No blockers

---
*Phase: 01-scaffold*
*Completed: 2026-03-13*
