---
phase: 01-scaffold
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .env.local
  - convex/_generated/
autonomous: false
requirements:
  - SCAF-12
  - SCAF-13

must_haves:
  truths:
    - ".env.local contains all required environment variables"
    - "Convex _generated/ types exist and reflect the shared schema"
    - "npm run dev starts on port 3001 without error"
  artifacts:
    - path: ".env.local"
      provides: "All environment variables for Convex, Stripe, Better Auth, n8n"
      contains: "CONVEX_DEPLOYMENT"
    - path: "convex/_generated/api.d.ts"
      provides: "Generated Convex API types from shared schema"
  key_links:
    - from: ".env.local"
      to: "convex/_generated/"
      via: "CONVEX_DEPLOYMENT env var triggers codegen"
      pattern: "CONVEX_DEPLOYMENT"
    - from: "convex/_generated/"
      to: "convex/schema.ts"
      via: "Generated types reflect schema definitions"
      pattern: "_generated"
---

<objective>
Create .env.local with all required environment variables, connect to the existing Convex deployment to generate types, and verify the dev server starts.

Purpose: Complete the scaffold by wiring the project to the shared Convex backend and verifying everything works end-to-end.
Output: Running dev server on port 3001 with Convex types generated.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
@.planning/phases/01-scaffold/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.local and generate Convex types</name>
  <files>.env.local, convex/_generated/</files>
  <action>
1. Create `.env.local` with all required environment variables. The user must provide the actual secret values. Create the file with placeholder structure:

```
# Convex — shared deployment
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=https://api.tenseats.io

# Better Auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=https://tenseats.io
NEXT_PUBLIC_APP_URL=https://tenseats.io

# Stripe
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# n8n
N8N_WEBHOOK_URL=

# Site
SITE_URL=https://tenseats.io
```

2. Check if the user's existing environment has `CONVEX_DEPLOYMENT` set (it may be in the main app's .env.local at `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/.env.local`). If readable, extract the CONVEX_DEPLOYMENT value and use it.

3. Similarly check for other env vars (BETTER_AUTH_SECRET, STRIPE keys) from the source .env.local and copy them if available.

4. Once CONVEX_DEPLOYMENT is set, run `npx convex dev --once` or `npx convex codegen` to generate `convex/_generated/` types from the shared schema. If codegen requires authentication, note it for the checkpoint.

IMPORTANT: Never commit .env.local to git. Add it to .gitignore if not already there.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && test -f .env.local && grep -q "CONVEX_DEPLOYMENT" .env.local && echo ".env.local exists with CONVEX_DEPLOYMENT"</automated>
  </verify>
  <done>.env.local created with all required env var keys. Values populated from source where available. Convex _generated/ types generated if CONVEX_DEPLOYMENT is valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify scaffold is complete and dev server starts</name>
  <action>
Present the user with verification steps for the complete scaffold. The user needs to confirm:
1. Dev server starts on port 3001
2. Convex types are generated
3. All env vars have real values (not placeholders)
  </action>
  <verify>User confirms dev server starts on port 3001 and Convex types exist</verify>
  <done>User approved the scaffold — dev server runs, Convex types generated, env vars populated.</done>
  <what-built>Complete project scaffold: Next.js project with all source files copied, dependencies installed, env vars configured, and Convex types generated.</what-built>
  <how-to-verify>
1. Run `npm run dev` from project root — it should start on port 3001 without crashing
2. Visit http://localhost:3001 — should show the placeholder page (not a Next.js error)
3. Check that `convex/_generated/` directory exists with type files
4. Verify `.env.local` has all required variables with real values (not placeholders)

If env vars have placeholder values, provide the real values now:
- CONVEX_DEPLOYMENT (from main app's .env.local or Convex dashboard)
- BETTER_AUTH_SECRET (from main app's .env.local)
- STRIPE_SECRET_KEY and STRIPE_PUBLISHABLE_KEY (from Stripe dashboard)
- STRIPE_WEBHOOK_SECRET (from Stripe dashboard)
- N8N_WEBHOOK_URL (from n8n instance)

If `npm run dev` fails with TypeScript errors, those will be addressed as the app pages are built in subsequent phases. The key check is that the server starts and doesn't crash on missing modules.
  </how-to-verify>
  <resume-signal>Type "approved" if dev server starts on 3001 and Convex types are generated. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<verification>
Dev server runs on port 3001. Convex _generated/ types exist. .env.local has all required variables.
</verification>

<success_criteria>
- .env.local exists with CONVEX_DEPLOYMENT, Stripe, Better Auth, n8n, and site URL variables
- convex/_generated/ directory exists with generated types
- `npm run dev` starts the server on port 3001
- No missing module errors prevent server startup
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-03-SUMMARY.md`
</output>
