---
phase: 08-why-tenseats-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/why/why-hero.tsx
  - components/why/why-problem.tsx
  - components/why/why-personas.tsx
autonomous: true
requirements: [WHY-01, WHY-02, WHY-03, WHY-04, WHY-08]

must_haves:
  truths:
    - "WhyHero displays 'Built different. On purpose.' headline"
    - "WhyProblem shows two-column layout with 'Sponsored. Optimized. Irrelevant.' vs 'The whisper, surfaced.'"
    - "WhyPersonas renders 6 role cards: Guest, Chef, Mixologist, Curator, Venue Host, Facilitator"
  artifacts:
    - path: "components/why/why-hero.tsx"
      provides: "Hero section with locked headline and atmospheric styling"
      exports: ["WhyHero"]
    - path: "components/why/why-problem.tsx"
      provides: "Two-column problem/solution layout"
      exports: ["WhyProblem"]
    - path: "components/why/why-personas.tsx"
      provides: "6 persona role cards"
      exports: ["WhyPersonas"]
  key_links:
    - from: "components/why/why-hero.tsx"
      to: "framer-motion"
      via: "motion animations"
      pattern: "motion\\."
    - from: "components/why/why-personas.tsx"
      to: "/join"
      via: "Link on persona cards"
      pattern: "href.*join"
---

<objective>
Create the first 3 of 5 Why Tenseats page components: WhyHero, WhyProblem, and WhyPersonas.

Purpose: Build the narrative top-half of the /why-tenseats brand story page — hero, problem/solution framing, and persona showcase.
Output: Three Client Components in components/why/ following established Tenseats styling patterns (Framer Motion animations, Tailwind, dark-theme-aware gradients).
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@components/join/join-hero.tsx
@components/join/food-as-language-section.tsx
@components/join/free-seat-section.tsx

<interfaces>
<!-- Established component patterns from join page and homepage -->

From components/join/join-hero.tsx:
- "use client" directive
- framer-motion for entrance animations (initial/animate/transition)
- Atmospheric gradient orbs pattern (pointer-events-none absolute divs with blur)
- Grain texture overlay SVG pattern
- Tailwind responsive: text-4xl sm:text-6xl md:text-7xl
- Dark-aware opacity: foreground/[0.08] dark:foreground/[0.03]
- lucide-react icons

From components/join/food-as-language-section.tsx:
- Section wrapper: max-w-6xl mx-auto px-6 sm:px-8 py-24 sm:py-32
- Grid layout: grid grid-cols-1 md:grid-cols-3
- whileInView animations with viewport={{ once: true }}
- Staggered delays: index * 0.15

From components/join/free-seat-section.tsx:
- Role cards with icon + title + description pattern
- Tab-based persona selection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhyHero component</name>
  <files>components/why/why-hero.tsx</files>
  <action>
Create components/why/why-hero.tsx as a "use client" component following the JoinHero pattern:

1. Atmospheric gradient orbs (3 layered divs with blur, dark-aware opacity — same pattern as JoinHero)
2. Grain texture overlay (same SVG pattern as JoinHero)
3. Eyebrow text: "The story behind the seat" — uppercase tracking-[0.25em] text-muted-foreground/70
4. Headline (LOCKED COPY): "Built different. On purpose." — split across two lines:
   - Line 1: "Built different."
   - Line 2 (muted): "On purpose."
   - Use same text scale as JoinHero: text-4xl sm:text-6xl md:text-7xl font-bold tracking-[-0.04em]
5. Subtext: "We didn't build another marketplace. We built the one that shouldn't need to exist — because the best food was never meant to be searched for." — text-base sm:text-lg md:text-xl text-muted-foreground max-w-lg mx-auto
6. No CTA button in hero (scroll-down chevron only, same as JoinHero)
7. All text animated with framer-motion staggered entrance (opacity+y, same delays as JoinHero)
8. Section height: min-h-[calc(100dvh-5rem)] flex items-center justify-center

Copy follows brand compliance: no forbidden adjectives, no "restaurant" in hero, discovery-mechanic language used.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/why/why-hero.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyHero component exists, exports WhyHero, displays "Built different. On purpose." headline with atmospheric styling matching site aesthetic</done>
</task>

<task type="auto">
  <name>Task 2: Create WhyProblem component</name>
  <files>components/why/why-problem.tsx</files>
  <action>
Create components/why/why-problem.tsx as a "use client" component:

1. Section wrapper: max-w-6xl mx-auto px-6 sm:px-8 py-24 sm:py-32
2. Two-column layout using grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16
3. Left column — "The Problem" (LOCKED COPY):
   - Column label: "The problem" — text-xs uppercase tracking-[0.2em] text-muted-foreground/60 mb-4
   - Headline: "Sponsored. Optimized. Irrelevant." — text-2xl sm:text-3xl md:text-4xl font-bold tracking-tight
   - Body text (3-4 sentences): Describe how algorithms surface paid placements, not genuine food. Trending lists reward SEO, not taste. The best meals were always found through a whisper, not a search bar. Use discovery-mechanic language.
   - Muted styling for body: text-muted-foreground text-sm sm:text-base leading-relaxed mt-6
4. Right column — "The Solution" (LOCKED COPY):
   - Column label: "The whisper, surfaced." — text-xs uppercase tracking-[0.2em] text-amber-500/70 mb-4
   - Headline: "The whisper, surfaced." — text-2xl sm:text-3xl md:text-4xl font-bold tracking-tight
   - Body text (3-4 sentences): Describe how Tenseats inverts the model — locals nominate, Insiders verify, access is earned not bought. No ads, no algorithms, no sponsored placements. Just the food that people actually talk about. Use "Insider" with capital I.
   - Amber accent on the label to differentiate from problem side
5. Both columns animate in with framer-motion whileInView (left from left, right from right — x offset instead of y)
6. A subtle divider between columns on desktop: a thin vertical line or gradient, hidden on mobile

Brand compliance: "Insider" capitalized, no forbidden adjectives, discovery-mechanic language.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/why/why-problem.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyProblem component exists with two-column layout showing "Sponsored. Optimized. Irrelevant." vs "The whisper, surfaced." headlines</done>
</task>

<task type="auto">
  <name>Task 3: Create WhyPersonas component</name>
  <files>components/why/why-personas.tsx</files>
  <action>
Create components/why/why-personas.tsx as a "use client" component:

1. Section wrapper: max-w-6xl mx-auto px-6 sm:px-8 py-24 sm:py-32
2. Section heading: "Who belongs here" — text-3xl sm:text-4xl font-bold tracking-tight text-center mb-4
3. Section subtext: "Every seat has a role. Every role has a voice." — text-muted-foreground text-center mb-16
4. 6 persona role cards in a grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6
5. Each card is a div (not a link) with:
   - Icon from lucide-react (choose appropriate icons per role):
     - Guest: Users icon
     - Chef: ChefHat icon
     - Mixologist: Wine icon (or GlassWater)
     - Curator: Compass icon
     - Venue Host: Building2 icon (or Home)
     - Facilitator: Handshake icon (or Network)
   - Role title: text-lg font-semibold
   - Role description (1-2 sentences each):
     - Guest: "You bring curiosity and an open palate. Every table needs someone ready to discover."
     - Chef: "You turn ingredients into stories. Your craft deserves a table that listens."
     - Mixologist: "You pair flavors with feelings. The bar is more than drinks — it's atmosphere."
     - Curator: "You find what others miss. Hidden menus, unlisted pop-ups, the places worth knowing."
     - Venue Host: "You create the space where it all happens. Your room sets the tone."
     - Facilitator: "You connect the dots. The chef to the venue, the guest to the experience."
   - Card styling: rounded-2xl border border-border/50 p-6 bg-card/50 hover:border-border transition-colors
6. Each card animates in with framer-motion whileInView, staggered by index * 0.1
7. A small "Find your seat" CTA link at bottom center linking to /join — using amber Button style matching JoinHero CTA pattern

Brand compliance: "Insider" capitalized if used, discovery-mechanic language, no forbidden adjectives.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/why/why-personas.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyPersonas component exists with 6 role cards (Guest, Chef, Mixologist, Curator, Venue Host, Facilitator), each with icon, title, and description</done>
</task>

</tasks>

<verification>
All 3 components compile without TypeScript errors:
```bash
npx tsc --noEmit components/why/why-hero.tsx components/why/why-problem.tsx components/why/why-personas.tsx
```
</verification>

<success_criteria>
- components/why/why-hero.tsx exports WhyHero with "Built different. On purpose." headline
- components/why/why-problem.tsx exports WhyProblem with two-column problem/solution layout
- components/why/why-personas.tsx exports WhyPersonas with 6 persona role cards
- All components use "use client", framer-motion animations, and Tailwind styling consistent with join page components
- All copy follows brand compliance rules (capital Insider, no forbidden adjectives, discovery language)
</success_criteria>

<output>
After completion, create `.planning/phases/08-why-tenseats-page/08-01-SUMMARY.md`
</output>
