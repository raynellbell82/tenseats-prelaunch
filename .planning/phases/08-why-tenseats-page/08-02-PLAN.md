---
phase: 08-why-tenseats-page
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - components/why/why-economics.tsx
  - components/why/why-cta.tsx
  - app/why-tenseats/page.tsx
autonomous: true
requirements: [WHY-01, WHY-05, WHY-06, WHY-07, WHY-08]

must_haves:
  truths:
    - "/why-tenseats renders all 5 sections: hero, problem/solution, personas, economics, CTA"
    - "WhyEconomics comparison table renders with $1.99 row and 4 competitor columns"
    - "Comparison table is usable and not overflowing at 375px viewport"
    - "WhyCta shows countdown widget and 'Become an Insider' CTA"
  artifacts:
    - path: "components/why/why-economics.tsx"
      provides: "Economics comparison table"
      exports: ["WhyEconomics"]
    - path: "components/why/why-cta.tsx"
      provides: "CTA section with countdown"
      exports: ["WhyCta"]
    - path: "app/why-tenseats/page.tsx"
      provides: "Page route assembling all 5 sections"
      exports: ["default"]
  key_links:
    - from: "app/why-tenseats/page.tsx"
      to: "components/why/*"
      via: "imports all 5 why components"
      pattern: "import.*components/why"
    - from: "app/why-tenseats/page.tsx"
      to: "components/landing/landing-header.tsx"
      via: "LandingHeader import"
      pattern: "import.*LandingHeader"
    - from: "components/why/why-cta.tsx"
      to: "convex/_generated/api"
      via: "useQuery for launchConfig countdown"
      pattern: "useQuery.*getLaunchConfig"
---

<objective>
Create WhyEconomics, WhyCta, and the /why-tenseats page route to complete the Why Tenseats page.

Purpose: Deliver the economics comparison (key conversion driver) and final CTA with countdown urgency, then wire all 5 sections into a routable page.
Output: Two Client Components + Server Component page route at app/why-tenseats/page.tsx.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-why-tenseats-page/08-01-SUMMARY.md

@app/join/page.tsx
@components/landing/hero-section.tsx
@components/launch/launch-countdown-full.tsx

<interfaces>
<!-- Components created by Plan 01 -->

From components/why/why-hero.tsx:
```typescript
export function WhyHero(): JSX.Element;
```

From components/why/why-problem.tsx:
```typescript
export function WhyProblem(): JSX.Element;
```

From components/why/why-personas.tsx:
```typescript
export function WhyPersonas(): JSX.Element;
```

<!-- Existing components needed -->

From components/landing/landing-header.tsx:
```typescript
export function LandingHeader(): JSX.Element;
```

From components/landing/hero-section.tsx (countdown pattern):
```typescript
// Uses useQuery(api.launch.queue.getLaunchConfig) to get deadline
// Renders LaunchCountdownFull with deadline prop
```

From components/launch/launch-countdown-full.tsx:
```typescript
interface LaunchCountdownFullProps { deadline: number; }
export function LaunchCountdownFull({ deadline }: LaunchCountdownFullProps): JSX.Element;
```

<!-- Page pattern from join -->

From app/join/page.tsx:
```typescript
// Server Component (no "use client")
// Imports LandingHeader + section components
// Returns fragment <> with components in order
export default function JoinPage(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhyEconomics component with mobile-responsive comparison table</name>
  <files>components/why/why-economics.tsx</files>
  <action>
Create components/why/why-economics.tsx as a "use client" component:

1. Section wrapper: max-w-6xl mx-auto px-6 sm:px-8 py-24 sm:py-32
2. Section heading: "The math is simple" — text-3xl sm:text-4xl font-bold tracking-tight text-center mb-4
3. Section subtext: "One price. Every seat. No platform tax." — text-muted-foreground text-center mb-16
4. Comparison table with these columns: Feature | Tenseats | Eventbrite | Tock | Luma
5. Table rows (at minimum):
   - "Per-seat price": $1.99 | $2.50+ fees | $3-5+ | Free (ad-supported)
   - "Platform fees": None | 3.7% + $1.79 | Commission | Free tier limits
   - "Ads / Sponsored": Never | Yes | Yes | Yes
   - "Data sold": Never | Yes | Yes | Yes
   - "Insider access": Included | N/A | N/A | N/A
   - "City-specific curation": 32 metros | Global generic | Restaurant-focused | Event-focused
6. Desktop layout (md+): Standard HTML table with:
   - Sticky first column (Feature names)
   - Tenseats column highlighted with amber-500/10 background and amber border-top
   - Clean borders: border-border/30
   - Header row: text-xs uppercase tracking-wider
7. Mobile layout (below md): Horizontally scrollable table wrapper with:
   - overflow-x-auto on the container
   - min-w-[600px] on the table element to prevent column crushing
   - A subtle left/right gradient fade hint at the edges showing more content is scrollable
   - Alternatively: Consider a card-based layout where each competitor is a separate card — but only if it's cleaner. The scrollable table is the safer bet for WHY-07.
8. Tenseats column values styled in amber-500 text to stand out
9. Checkmarks (Check icon from lucide) for Tenseats positives, X marks (X icon) for competitor negatives
10. Animate in with framer-motion whileInView

CRITICAL for WHY-07: The table MUST be usable at 375px — no horizontal overflow on the page itself. The table scrolls within its container, the page does not scroll horizontally.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/why/why-economics.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyEconomics component exports, renders comparison table with $1.99 Tenseats row and 4 competitor columns, table is scrollable on mobile without page overflow</done>
</task>

<task type="auto">
  <name>Task 2: Create WhyCta component with countdown and CTA</name>
  <files>components/why/why-cta.tsx</files>
  <action>
Create components/why/why-cta.tsx as a "use client" component:

1. Section wrapper: py-24 sm:py-32 text-center — full-width atmospheric background similar to WhyHero (gradient orbs, but simpler — 1-2 orbs only)
2. Headline (LOCKED COPY): "The Insiders are already inside." — text-3xl sm:text-5xl md:text-6xl font-bold tracking-tight
3. Subtext: "Founding memberships are limited. The countdown is real." — text-muted-foreground text-base sm:text-lg mt-6
4. Countdown widget: Import and render LaunchCountdownFull from @/components/launch/launch-countdown-full
   - Use useQuery(api.launch.queue.getLaunchConfig) to get the launch deadline (same pattern as HeroSection in components/landing/hero-section.tsx)
   - If config is undefined (loading), show a pulsing placeholder
   - If config.launchDate exists, pass it as deadline prop
   - Center the countdown: flex justify-center mt-10
5. CTA button: "Become an Insider" linking to /signup
   - Amber outline style matching JoinHero: rounded-full border-amber-500/30 text-amber-500 hover:bg-amber-500/10 hover:text-amber-400 hover:border-amber-500/50
   - Sparkles icon on left, ArrowRight icon on right
   - mt-10
6. Framer-motion entrance animations on headline, subtext, countdown, and CTA (staggered)

Brand compliance: "Insider" with capital I (appears in headline and CTA), discovery-mechanic language.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/why/why-cta.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyCta component exports, displays "The Insiders are already inside." headline, countdown from Convex launchConfig, and "Become an Insider" CTA linking to /signup</done>
</task>

<task type="auto">
  <name>Task 3: Create /why-tenseats page route</name>
  <files>app/why-tenseats/page.tsx</files>
  <action>
Create app/why-tenseats/page.tsx as a Server Component (NO "use client") following the exact join page pattern:

1. Import LandingHeader from @/components/landing/landing-header
2. Import all 5 Why components:
   - WhyHero from @/components/why/why-hero
   - WhyProblem from @/components/why/why-problem
   - WhyPersonas from @/components/why/why-personas
   - WhyEconomics from @/components/why/why-economics
   - WhyCta from @/components/why/why-cta
3. Export default function WhyTenseatsPage returning a fragment with components in order:
   - LandingHeader
   - WhyHero
   - WhyProblem
   - WhyPersonas
   - WhyEconomics
   - WhyCta
4. No wrapper divs, no extra styles — each component manages its own layout (same pattern as join page)
5. Add metadata export for SEO:
   ```typescript
   export const metadata = {
     title: "Why Tenseats | Built Different. On Purpose.",
     description: "Discover why Tenseats exists — no ads, no algorithms, just the food that people actually talk about. $1.99 per seat, 32 cities, Insider access.",
   };
   ```
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit app/why-tenseats/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>/why-tenseats route exists as Server Component rendering LandingHeader + WhyHero + WhyProblem + WhyPersonas + WhyEconomics + WhyCta in order with SEO metadata</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit app/why-tenseats/page.tsx components/why/why-economics.tsx components/why/why-cta.tsx`
2. Dev server renders: `curl -s http://localhost:3001/why-tenseats | grep -c "Built different"` returns 1
3. All 5 sections present in page output
</verification>

<success_criteria>
- /why-tenseats renders all 5 sections in order: hero, problem, personas, economics, CTA
- Economics comparison table shows $1.99 Tenseats row with Eventbrite, Tock, Luma columns
- Table is scrollable on mobile (375px) without page-level horizontal overflow
- Countdown widget renders from Convex launchConfig data
- "Become an Insider" CTA links to /signup
- Page follows Server Component pattern from join page (no "use client" at page level)
- All copy follows brand compliance: capital Insider, no forbidden adjectives, discovery language
</success_criteria>

<output>
After completion, create `.planning/phases/08-why-tenseats-page/08-02-SUMMARY.md`
</output>
