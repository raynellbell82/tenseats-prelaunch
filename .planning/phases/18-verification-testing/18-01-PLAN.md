---
phase: 18-verification-testing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "getMyMembership query returns correct tier/isLifetime/isActive for early_bird, founding, and insider tier values"
    - "MembershipStatusCard renders 'Early Bird' label and 'Lifetime Member' badge for early_bird tier"
    - "MembershipStatusCard renders 'Founding' label and 'Lifetime Member' badge for founding tier"
    - "MembershipStatusCard renders 'Insider' label and 'Insider' badge for insider tier"
    - "ManageBillingButton only appears for insider tier, not for early_bird or founding"
    - "PaymentHistoryList only renders for insider tier"
  artifacts:
    - path: "convex/billing/membership.ts"
      provides: "getMyMembership query"
      contains: "early_bird.*founding"
    - path: "components/account/membership-status-card.tsx"
      provides: "TIER_LABELS map and conditional rendering"
      contains: "TIER_LABELS"
    - path: "components/account/membership-tier-badge.tsx"
      provides: "Lifetime vs subscription badge"
      contains: "Lifetime Member"
    - path: "app/account/membership/page.tsx"
      provides: "Page with conditional Insider sections"
      contains: "isInsider"
  key_links:
    - from: "app/account/membership/page.tsx"
      to: "convex/billing/membership.ts"
      via: "useQuery(api.billing.membership.getMyMembership)"
      pattern: "api\\.billing\\.membership\\.getMyMembership"
    - from: "components/account/membership-status-card.tsx"
      to: "components/account/membership-tier-badge.tsx"
      via: "import MembershipTierBadge"
      pattern: "MembershipTierBadge"
---

<objective>
Verify that all three membership tier types (Early Bird, Founding, Insider) display correctly on the membership page — correct tier names, badges, and available actions.

Purpose: Confirm TEST-01 — the frontend components built in Phase 16 correctly differentiate the three tier types when backed by real Convex data.
Output: Verification evidence that tier display logic is correct across all paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-membership-frontend/16-01-SUMMARY.md
@.planning/phases/16-membership-frontend/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated tier display verification via code analysis and build check</name>
  <files></files>
  <read_first>
    - convex/billing/membership.ts
    - components/account/membership-status-card.tsx
    - components/account/membership-tier-badge.tsx
    - components/account/manage-billing-button.tsx
    - components/account/payment-history-list.tsx
    - app/account/membership/page.tsx
    - app/launch/success/page.tsx
  </read_first>
  <action>
Run a systematic verification of TEST-01 requirements by checking all tier display paths through the code:

**1. Build verification:**
Run `npx next build` (or `npm run build`) to confirm the app compiles without errors. This proves all imports resolve, TypeScript types align, and the membership page is deployable.

**2. Tier label mapping verification:**
Confirm `components/account/membership-status-card.tsx` contains the TIER_LABELS map with all three tiers:
- `early_bird: "Early Bird"`
- `founding: "Founding"`
- `insider: "Insider"`

**3. Badge differentiation verification:**
Confirm `components/account/membership-tier-badge.tsx`:
- Lifetime path (isLifetime=true): uses `bg-accent-burnt-sienna` class and text "Lifetime Member"
- Subscription path (isLifetime=false): uses `variant="secondary"` and text "Insider"

**4. Conditional action verification:**
Confirm `components/account/membership-status-card.tsx`:
- ManageBillingButton only renders inside `{isInsider && (...)}` gate (line 75-79)
- Lifetime tiers get `ring-2 ring-foreground` border class (line 63)

Confirm `app/account/membership/page.tsx`:
- PaymentHistoryList only renders inside `{isInsider && (...)}` gate (line 84-90)
- `isInsider` is derived from `membership?.tier === "insider"` (line 64)

**5. Query return shape verification:**
Confirm `convex/billing/membership.ts`:
- `getMyMembership` returns `{tier, isActive, isLifetime, hasBillingCustomer}`
- `isLifetime` is computed as `tier === "early_bird" || tier === "founding"` (line 29)

**6. Success page CTA verification:**
Confirm `app/launch/success/page.tsx` contains:
- Link with `href="/account/membership"`
- Text "Manage your membership"

Report all findings. If any check fails, document exactly what is wrong.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0 (no compilation errors)
    - `components/account/membership-status-card.tsx` contains `early_bird: "Early Bird"` and `founding: "Founding"` and `insider: "Insider"` in TIER_LABELS
    - `components/account/membership-tier-badge.tsx` contains `bg-accent-burnt-sienna` and `"Lifetime Member"` and `variant="secondary"` and `"Insider"`
    - `components/account/membership-status-card.tsx` contains `isInsider &&` gating ManageBillingButton
    - `components/account/membership-status-card.tsx` contains `ring-2 ring-foreground` for lifetime border
    - `app/account/membership/page.tsx` contains `isInsider &&` gating PaymentHistoryList
    - `convex/billing/membership.ts` contains `tier === "early_bird" || tier === "founding"` for isLifetime derivation
    - `app/launch/success/page.tsx` contains `href="/account/membership"` and `Manage your membership`
  </acceptance_criteria>
  <done>All tier display code paths verified: three tier labels mapped, badge differentiation confirmed, Insider-only actions gated, lifetime border applied, and build succeeds</done>
</task>

</tasks>

<verification>
- Build passes without errors
- All three tier paths (early_bird, founding, insider) have correct label mappings
- Badge component differentiates lifetime from subscription visually
- ManageBillingButton and PaymentHistoryList are gated behind insider-only conditions
- Success page CTA links to membership page
</verification>

<success_criteria>
TEST-01 is verified: code analysis confirms all tier types display correctly with the right tier name, badge variant, and available actions. Build succeeds proving no broken imports or type errors.
</success_criteria>

<output>
After completion, create `.planning/phases/18-verification-testing/18-01-SUMMARY.md`
</output>
