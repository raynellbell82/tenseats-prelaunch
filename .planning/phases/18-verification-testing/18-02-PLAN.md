---
phase: 18-verification-testing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "Insider member can click Manage Billing and reach Stripe Customer Portal"
    - "Customer Portal shows options to update payment method or cancel subscription"
    - "Changes made in Customer Portal are reflected in Stripe without errors"
    - "A new Insider checkout in Stripe test mode completes successfully"
    - "After checkout, the new subscriber's billing customer record appears in the billing component automatically"
    - "No manual backfill is required for the new subscriber"
  artifacts:
    - path: "components/account/manage-billing-button.tsx"
      provides: "Button that triggers createBillingPortalSession action"
      contains: "createBillingPortalSession"
    - path: "convex/billing/subscriptions.ts"
      provides: "createBillingPortalSession and syncMyBillingCustomer actions"
      contains: "createBillingPortalSession"
    - path: "app/account/membership/page.tsx"
      provides: "Lazy sync via syncMyBillingCustomer on Insider page load"
      contains: "syncMyBillingCustomer"
  key_links:
    - from: "components/account/manage-billing-button.tsx"
      to: "convex/billing/subscriptions.ts"
      via: "useAction(api.billing.subscriptions.createBillingPortalSession)"
      pattern: "createBillingPortalSession"
    - from: "app/account/membership/page.tsx"
      to: "convex/billing/subscriptions.ts"
      via: "useAction(api.billing.subscriptions.syncMyBillingCustomer)"
      pattern: "syncMyBillingCustomer"
---

<objective>
Verify the Customer Portal end-to-end flow (TEST-02) and new Insider checkout auto-sync (TEST-03) in the live environment with Stripe test mode.

Purpose: Confirm that the billing infrastructure from Phases 15-17 works end-to-end — portal opens, changes persist, and new checkouts auto-register without manual backfill.
Output: Human-verified evidence that both flows work correctly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-membership-frontend/16-01-SUMMARY.md
@.planning/phases/16-membership-frontend/16-02-SUMMARY.md
@.planning/phases/17-ops-configuration/17-01-SUMMARY.md
@.planning/phases/17-ops-configuration/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify billing backend wiring for portal and sync paths</name>
  <files></files>
  <read_first>
    - convex/billing/subscriptions.ts
    - convex/billing/billingHelpers.ts
    - components/account/manage-billing-button.tsx
    - app/account/membership/page.tsx
  </read_first>
  <action>
Verify the two critical backend paths exist and are correctly wired before human testing:

**1. Customer Portal path (TEST-02):**
Confirm `convex/billing/subscriptions.ts` exports `createBillingPortalSession`:
- It must be an action (not a query or mutation)
- It must accept a `returnUrl` argument
- It must return an object with a `url` property (the portal session URL)

Confirm `components/account/manage-billing-button.tsx`:
- Calls `createBillingPortalSession({ returnUrl: window.location.href })`
- On success, sets `window.location.href = url` to redirect
- On failure, shows toast error "Could not open billing portal. Try again."

**2. Auto-sync path (TEST-03):**
Confirm `convex/billing/subscriptions.ts` exports `syncMyBillingCustomer`:
- It must be an action
- It syncs the current user's billing customer record to the stripe component

Confirm `app/account/membership/page.tsx`:
- `syncMyBillingCustomer` is called via `useAction` on first Insider page load
- Guarded by `useRef(false)` to fire exactly once
- Called with empty args `{}`

**3. Post-fulfillment bridge path (TEST-03):**
Check that `convex/billing/subscriptions.ts` or a related file contains the post-fulfillment sync logic that fires when a new Insider checkout completes (the `membershipWebhooks.ts` bridge from Phase 15).

Report whether each path is wired correctly or has issues.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && grep -n "createBillingPortalSession\|syncMyBillingCustomer" convex/billing/subscriptions.ts | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `convex/billing/subscriptions.ts` contains `export const createBillingPortalSession` as an action
    - `convex/billing/subscriptions.ts` contains `export const syncMyBillingCustomer` as an action
    - `components/account/manage-billing-button.tsx` contains `createBillingPortalSession({ returnUrl: window.location.href })`
    - `components/account/manage-billing-button.tsx` contains `window.location.href = url`
    - `components/account/manage-billing-button.tsx` contains `"Could not open billing portal. Try again."`
    - `app/account/membership/page.tsx` contains `syncMyBillingCustomer({})` with `.catch()`
    - `app/account/membership/page.tsx` contains `useRef(false)` for fire-once guard
  </acceptance_criteria>
  <done>Both billing backend paths (portal session + auto-sync) are confirmed wired in the codebase</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Customer Portal flow (TEST-02)</name>
  <files></files>
  <action>
Human verification of the Stripe Customer Portal end-to-end flow. No code changes needed — this is a live environment test.

**Prerequisites:** An Insider test account with an active subscription in Stripe test mode.

**Steps:**
1. Log in as the Insider test account
2. Navigate to `/account/membership`
3. Confirm the page shows: Tier label "Insider", badge in secondary variant (not "Lifetime Member"), status "Active", "Manage Billing" button in card footer, payment history section below
4. Click "Manage Billing"
5. Confirm button shows spinner + "Opening..." text, then browser redirects to Stripe Customer Portal
6. Confirm portal shows options for: updating payment method, viewing invoices, cancelling subscription
7. Update the payment method to a different Stripe test card (e.g., `4242 4242 4242 4242` to `5555 5555 5555 4444`)
8. Return to the app via portal redirect back to membership page
9. Verify in Stripe Dashboard that the payment method was updated without errors

**If Manage Billing fails:** Check that STRIPE_BILLING_WEBHOOK_SECRET is set in Convex env vars, and that the Customer Portal is configured in Stripe Dashboard (Phase 17-01).
  </action>
  <verify>
    <automated>echo "Human verification — no automated check available for live Stripe portal flow"</automated>
  </verify>
  <acceptance_criteria>
    - Manage Billing button redirects to Stripe Customer Portal without errors
    - Portal displays payment method update, invoice access, and cancellation options
    - Payment method change persists in Stripe Dashboard after portal interaction
  </acceptance_criteria>
  <done>Insider test account completes Customer Portal flow — opens portal, updates payment method, change reflected in Stripe without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — New Insider checkout auto-sync (TEST-03)</name>
  <files></files>
  <action>
Human verification of the new Insider checkout auto-sync flow. No code changes needed — this is a live environment test.

**Prerequisites:** A test account that does NOT yet have an Insider subscription.

**Steps:**
1. Complete a new Insider checkout flow using Stripe test mode:
   - Navigate to the launch/join flow
   - Select Insider tier
   - Complete checkout with Stripe test card `4242 4242 4242 4242`
   - Arrive at the success page
2. On the success page, click "Manage your membership" CTA link
3. Confirm the membership page shows: Tier "Insider", Status "Active", "Manage Billing" button visible
4. **Critical check — auto-sync verification:**
   - Open Stripe Dashboard > Customers
   - Verify the new subscriber has a billing customer record created automatically
   - This should have happened via the post-fulfillment webhook bridge (no manual backfill needed)
5. Verify the payment history section shows the initial charge

**If auto-sync did not work:**
- Check Convex logs for `syncCustomerToComponent` or `membershipWebhooks` activity
- Check Stripe Dashboard > Developers > Webhooks for billing webhook delivery status
- The lazy sync on page load (`syncMyBillingCustomer`) is a fallback if the webhook bridge didn't fire
  </action>
  <verify>
    <automated>echo "Human verification — no automated check available for live Stripe checkout + auto-sync flow"</automated>
  </verify>
  <acceptance_criteria>
    - New Insider checkout completes successfully in Stripe test mode
    - Billing customer record appears in Stripe component automatically after checkout
    - No manual backfill script execution was required
    - Membership page shows correct Insider tier, active status, and payment history for the new subscriber
  </acceptance_criteria>
  <done>New Insider checkout auto-syncs via post-fulfillment bridge — subscriber appears in billing component records automatically without running backfill</done>
</task>

</tasks>

<verification>
- Customer Portal opens from Manage Billing button without redirect errors
- Payment method or cancellation changes in portal persist in Stripe
- New Insider checkout creates billing customer record automatically
- No manual backfill required for new subscribers
- Membership page displays correctly for the newly created Insider
</verification>

<success_criteria>
TEST-02 is verified: Insider test account completes Customer Portal flow — opens portal, updates payment method or cancels, change reflected in Stripe without errors.
TEST-03 is verified: New Insider checkout completes in test mode and subscriber appears in billing component records automatically, without running backfill manually.
</success_criteria>

<output>
After completion, create `.planning/phases/18-verification-testing/18-02-SUMMARY.md`
</output>
