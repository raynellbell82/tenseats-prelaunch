---
phase: 23-guest-success-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/launch/success/guest/page.tsx
  - components/post-signup/social-links.tsx
autonomous: false
requirements: [GSUCCESS-01, GSUCCESS-02, GSUCCESS-03]

must_haves:
  truths:
    - "After OTP verification, a guest user sees a dedicated success page with community welcome messaging"
    - "The page displays a reminder to add supportteams@tenseats.io to their address book with click-to-copy"
    - "The page shows Instagram and Pinterest social links with custom icons from Phase 21"
    - "The page uses PostSignupLayout and matches the Phase 22 design language"
  artifacts:
    - path: "app/launch/success/guest/page.tsx"
      provides: "Full guest success page with auth guard, welcome copy, timeline, social links"
      min_lines: 80
    - path: "components/post-signup/social-links.tsx"
      provides: "Shared SocialLinks component reusable by Phase 24 vendor success"
      exports: ["SocialLinks"]
  key_links:
    - from: "app/launch/success/guest/page.tsx"
      to: "components/post-signup/post-signup-layout.tsx"
      via: "import PostSignupLayout, itemVariants"
      pattern: "import.*PostSignupLayout.*itemVariants"
    - from: "app/launch/success/guest/page.tsx"
      to: "components/post-signup/vertical-timeline.tsx"
      via: "import VerticalTimeline"
      pattern: "VerticalTimeline"
    - from: "app/launch/success/guest/page.tsx"
      to: "components/post-signup/social-links.tsx"
      via: "import SocialLinks"
      pattern: "SocialLinks"
    - from: "components/post-signup/social-links.tsx"
      to: "components/icons/social-icons.tsx"
      via: "import InstagramIcon, PinterestIcon"
      pattern: "InstagramIcon.*PinterestIcon"
    - from: "app/launch/success/guest/page.tsx"
      to: "lib/auth-client"
      via: "useSession for auth guard"
      pattern: "useSession"
---

<objective>
Build the guest success page at `/launch/success/guest` — replacing the existing stub with a full branded page that welcomes verified guests into the Tenseats community, reminds them to safelist the support email, and connects them to social channels.

Purpose: This is the first thing a guest sees after OTP verification. It needs to feel like a moment of belonging, not a transactional confirmation.
Output: Full guest success page + shared SocialLinks component (reusable by Phase 24)
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-guest-success-page/23-CONTEXT.md
@.planning/phases/22-verification-page/22-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From components/post-signup/post-signup-layout.tsx:
```typescript
export const itemVariants: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: "easeOut" as const } },
};
export function PostSignupLayout({ children }: { children: ReactNode }): JSX.Element;
// Dark bg, LandingHeader, centered max-w-2xl, staggerChildren: 0.15
```

From components/post-signup/vertical-timeline.tsx:
```typescript
interface TimelineStep {
  label: string;
  detail?: string;
  active?: boolean;
}
export function VerticalTimeline({ steps }: { steps: TimelineStep[] }): JSX.Element;
```

From components/icons/social-icons.tsx:
```typescript
export function InstagramIcon({ className = "h-5 w-5" }: { className?: string }): JSX.Element;
export function PinterestIcon({ className = "h-5 w-5" }: { className?: string }): JSX.Element;
// Monoline SVG, stroke="currentColor", strokeWidth="1.5", fill="none"
```

From lib/auth-client (useSession pattern used in verify-email page):
```typescript
const { data: session, isPending } = useSession();
// session.user.emailVerified — boolean for OTP completion check
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build guest success page and shared SocialLinks component</name>
  <files>app/launch/success/guest/page.tsx, components/post-signup/social-links.tsx</files>
  <action>
**1. Create `components/post-signup/social-links.tsx` — shared component for Phase 23 and 24:**

A "use client" component that renders a "Connect with us" section with:
- Section heading: "Connect with us" (or similar — Claude's discretion on exact wording)
- Two social link items, each with: larger icon (h-6 w-6 or h-7 w-7), text label ("Instagram", "Pinterest"), and URL
- Instagram URL: `https://www.instagram.com/tenseats`
- Pinterest URL: `https://pinterest.com/tenseats`
- All links: `target="_blank"` and `rel="noopener noreferrer"`
- Import `InstagramIcon` and `PinterestIcon` from `@/components/icons/social-icons`
- Style: flex row with gap, centered, text in `text-muted-foreground` with hover to `text-foreground` transition
- Component accepts no props — URLs are static constants within the component

**2. Replace stub at `app/launch/success/guest/page.tsx` with full page:**

"use client" page with these sections, all wrapped in `PostSignupLayout` from `@/components/post-signup/post-signup-layout`:

**Auth guard:**
- Use `useSession()` from `@/lib/auth-client`
- If `isPending`, return null (or minimal loading state inside PostSignupLayout)
- If no session or `!session.user.emailVerified`, redirect to `/launch` via `router.push("/launch")` in a useEffect
- Only render page content when session exists and emailVerified is true

**Green check circle with pulse (reuse verify page pattern):**
- Wrap in `motion.div variants={itemVariants}`
- Same structure as verify page: outer div with `bg-green-500/10 animate-pulse` (2.5s duration), inner div with `bg-green-500/20`, `CheckCircle2` icon from lucide-react in `text-green-500`
- Size: h-20 w-20 outer, h-10 w-10 icon (same as verify page)

**Headline + body copy:**
- Wrap in `motion.div variants={itemVariants}`
- Generate headline using tenseats-copy-writer skill. Requirements:
  - Pattern 5 (Hospitality-Coded Welcome) is the primary fit — this is a post-signup welcome moment
  - Table metaphor in headline per user decision: "Your seat at the table is set" direction
  - 5-8 words, direct 2nd person
  - Must feel like community belonging, not transactional confirmation
  - Generate 4 options, select the strongest. Comment the other options in the source code (same pattern as verify page)
- Body copy: 1-2 sentences, community-focused. Blend warmth with the "you're one of us" tone from CONTEXT.md. Use brand voice: evocative + direct, no forbidden adjectives.
- Style: headline in `text-4xl md:text-5xl font-bold tracking-tight text-foreground`, body in `text-lg text-muted-foreground max-w-md mx-auto`

**VerticalTimeline section (next steps):**
- Wrap in `motion.div variants={itemVariants}`
- Center the timeline (same `flex justify-center` + `text-left w-48` pattern as verify page)
- Three steps per CONTEXT.md decisions:
  1. `{ label: "Add us to your contacts", detail: "supportteams@tenseats.io", active: true }` — with a click-to-copy button
  2. `{ label: "Follow us on social", detail: "Stay in the loop", active: true }`
  3. `{ label: "Explore soon", detail: "Something's cooking", active: false }` — teaser, no link

**Click-to-copy for email address:**
- The email safelist step in the timeline needs a small copy button next to or below the email address
- Implementation approach: Since VerticalTimeline is a reusable component that shouldn't be modified for one page's needs, render the click-to-copy UI OUTSIDE the VerticalTimeline — as a supplementary element. Options:
  - Place a small "Copy email" button with a clipboard icon (from lucide-react: `Copy` or `ClipboardCopy`) directly below the VerticalTimeline's first step area, or
  - Render the full email address as a separate styled element below the timeline with a copy button
  - On click: `navigator.clipboard.writeText("supportteams@tenseats.io")` then show brief feedback (icon changes to checkmark for 2 seconds, or a small "Copied!" text flash)
- Claude's discretion on exact placement and feedback UX — keep it minimal and elegant

**SocialLinks section:**
- Wrap in `motion.div variants={itemVariants}`
- Render the `SocialLinks` component from `@/components/post-signup/social-links`
- This is a standalone section below the timeline per CONTEXT.md: "separate section with visual weight"

**No CTA button at bottom** — per user decision, the page is a destination. Social links are the only actions.

**Design consistency with Phase 22:**
- Same `space-y-8` vertical rhythm as verify page
- Same text sizing scale (text-4xl/5xl headline, text-lg body)
- Same green-500 accent color for check circle and active timeline dots
- Framer Motion stagger via PostSignupLayout + itemVariants on each section
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Guest success page renders at /launch/success/guest with PostSignupLayout wrapper
- Auth guard redirects unverified users to /launch
- Green check pulse animation matches verify page
- Headline uses table metaphor + community tone (copy-writer skill applied, options commented)
- VerticalTimeline shows 3 guest-specific steps with email safelist as step 1
- Click-to-copy for supportteams@tenseats.io works with visual feedback
- SocialLinks component renders Instagram + Pinterest with custom icons and text labels
- SocialLinks component is in components/post-signup/ and importable by Phase 24
- All sections animate via itemVariants stagger
- TypeScript compiles with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and copy review of guest success page</name>
  <action>Human reviews the completed guest success page for design quality, copy tone, and interaction correctness</action>
  <what-built>Complete guest success page at /launch/success/guest with branded welcome copy, email safelist timeline, click-to-copy, and social links section using custom Instagram/Pinterest icons</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:3001/launch/success/guest
    2. If redirected to /launch (auth guard), complete a queue signup + OTP flow first, or temporarily comment out the auth guard to preview
    3. Verify the page matches Phase 22 design language: dark background, staggered entrance animations, green check pulse
    4. Read the headline and body copy — does it feel like "you're one of us" community belonging? Is the table metaphor natural?
    5. Check the VerticalTimeline: 3 steps visible, email safelist is step 1, "explore soon" is inactive/teaser
    6. Test click-to-copy: click the copy button next to supportteams@tenseats.io, paste somewhere to confirm
    7. Check the social links section: Instagram and Pinterest with custom monoline icons, text labels, links open in new tabs
    8. Resize to mobile — layout should be responsive (single column, no overflow)
  </how-to-verify>
  <verify>Visual checkpoint — human approval required</verify>
  <done>User approves page design, copy tone, and all interactions</done>
  <resume-signal>Type "approved" or describe issues with copy, layout, or interactions</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `/launch/success/guest` renders full page (not stub)
- `SocialLinks` component exists at `components/post-signup/social-links.tsx` and is importable
- Auth guard prevents unverified access
- Copy follows brand voice (no forbidden adjectives, table metaphor present, community tone)
</verification>

<success_criteria>
- Guest success page fully replaces stub with branded community welcome
- supportteams@tenseats.io is prominently displayed with click-to-copy functionality
- Instagram and Pinterest social links render with custom SVG icons
- Page design is visually consistent with /launch/verify (same layout system, animations, color palette)
- SocialLinks component is extracted and ready for Phase 24 vendor success page to import
</success_criteria>

<output>
After completion, create `.planning/phases/23-guest-success-page/23-01-SUMMARY.md`
</output>
