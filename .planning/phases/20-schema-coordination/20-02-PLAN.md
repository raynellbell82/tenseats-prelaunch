---
phase: 20-schema-coordination
plan: 02
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - convex/schema.ts
autonomous: false
requirements:
  - SCHEMA-02

must_haves:
  truths:
    - "The shared Convex backend has any schema additions from the prompt document deployed and queryable"
    - "The prelaunch site's schema.ts is a copy of the main app's current deployed schema"
    - "No onboarding persistence code is written until schema deployment is confirmed"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Updated schema copied from main app (if changes were needed)"
      contains: "defineSchema"
  key_links:
    - from: "convex/schema.ts"
      to: "main app convex/schema.ts"
      via: "full file copy from main app repo"
      pattern: "defineSchema"
---

<objective>
Verify that the shared Convex schema has been updated (if needed) after the user runs the prompt document from Plan 01 against the main app repo. Copy the updated schema.ts back to the prelaunch site so both repos stay in sync.

Purpose: SCHEMA-02 is a gate — Phase 25 (Onboarding Persistence) cannot start until schema deployment is confirmed. This plan ensures the shared backend is ready.

Output: Updated `convex/schema.ts` (if schema changed) and confirmation that the deployment is live
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-coordination/20-CONTEXT.md
@.planning/phases/20-schema-coordination/20-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User runs prompt in main app repo and deploys schema</name>
  <files>n/a</files>
  <action>
Plan 01 generated `tenseats-main-app-post-signup-prompt.md` — a comprehensive Claude Code prompt for the main app.

The user must now:
1. Open the main Tenseats marketplace repo in Cursor or Claude Code
2. Reference or paste `tenseats-main-app-post-signup-prompt.md` as the prompt
3. Let it implement the post-signup experience (or at minimum, any schema changes)
4. If schema changes were made, deploy with `npx convex deploy`
5. Return here and confirm one of:
   - "schema deployed" — changes were made and deployed
   - "no schema changes needed" — existing fields were sufficient
   - "skipped for now" — deferring main app work, proceed with prelaunch phases using current schema
  </action>
  <verify>User provides one of the three confirmation signals</verify>
  <done>User has confirmed the schema deployment status</done>
</task>

<task type="auto">
  <name>Task 2: Copy updated schema and verify deployment</name>
  <files>convex/schema.ts</files>
  <action>
Based on the user's response from Task 1:

**If "schema deployed" or "no schema changes needed":**
1. Copy the full `schema.ts` from the main app repo at `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/convex/schema.ts` to `convex/schema.ts` in this repo (overwrite entirely — full file copy, no partial diffs, per established pattern)
2. Run `npx convex dev --once` to verify the schema is compatible with the shared Convex deployment (this validates without starting the dev server)
3. If the command succeeds, schema is verified

**If "skipped for now":**
1. Do NOT copy schema — leave current schema.ts as-is
2. Note in the summary that SCHEMA-02 is deferred but Phase 25 cannot proceed until this is resolved
3. Phases 21-24 can proceed since they don't depend on schema changes

**Important:** The prelaunch site does NOT deploy its own schema — it uses the schema deployed by the main app. The copy is for type safety and local development only.
  </action>
  <verify>
    <automated>test -f convex/schema.ts && head -1 convex/schema.ts | grep -q "import" && echo "PASS: schema.ts exists and starts with import" || echo "FAIL: schema.ts missing or malformed"</automated>
  </verify>
  <done>convex/schema.ts in prelaunch repo matches the main app's deployed schema, OR a clear note exists that schema verification was deferred with impact documented</done>
</task>

</tasks>

<verification>
1. User confirmed schema deployment status
2. `convex/schema.ts` matches main app (if deployed) or is unchanged (if skipped)
3. Summary documents the gate status for Phase 25
</verification>

<success_criteria>
The shared Convex backend's schema status is confirmed and documented — either deployed with any needed additions, confirmed as already sufficient, or explicitly deferred with downstream impact noted. Phase 25 knows whether it can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-coordination/20-02-SUMMARY.md`
</output>
