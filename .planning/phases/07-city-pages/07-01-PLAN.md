---
phase: 07-city-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/city-data.ts
autonomous: true
requirements: [CITY-08, CITY-09, CITY-10]

must_haves:
  truths:
    - "lib/city-data.ts exports CityData type with all required fields"
    - "lib/city-data.ts exports METROS_DATA array with all 32 metro slugs"
    - "lib/city-data.ts exports CITY_DATA record with exactly 32 entries keyed by slug"
    - "Every CITY_DATA entry has heroHeadline, heroSubhead, metaTagline, metaDescription, sceneIntro, 3 sceneBlocks, topPersonas array, and tier"
    - "All heroSubhead values are under 25 words"
  artifacts:
    - path: "lib/city-data.ts"
      provides: "CityData type, METROS_DATA array, CITY_DATA record for all 32 metros"
      min_lines: 400
  key_links:
    - from: "lib/city-data.ts"
      to: "convex/metros.ts"
      via: "METROS_DATA slug names match METROS_DATA[n].name in Convex"
      pattern: "METROS_DATA"
---

<objective>
Create the city data file with CityData type definition and all 32 metro city entries containing hero copy, scene blocks, persona cards, SEO metadata, and tier classification.

Purpose: This data file is the foundation for all 32 city pages — every component reads from it. The copy has been pre-written in the project prompt and must be transcribed exactly.
Output: lib/city-data.ts with exported CityData type, METROS_DATA array, and CITY_DATA record.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tenseats-prelaunch-claude-code-prompt.md
@convex/metros.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CityData type and METROS_DATA re-export</name>
  <files>lib/city-data.ts</files>
  <action>
Create lib/city-data.ts with:

1. Export the CityData type exactly as specified in the project prompt (City Copy Specification section):
```typescript
export type CityData = {
  slug: string;               // matches METROS_DATA[n].name
  displayName: string;        // e.g. "Chicago, IL"
  state: string;
  heroHeadline: string;       // 5-8 words
  heroSubhead: string;        // 15-25 words
  metaTagline: string;        // for page title
  metaDescription: string;    // 150 chars for SEO
  sceneIntro: string;         // 30-50 words, describes the food scene
  sceneBlocks: [
    { heading: string; description: string; unsplashQuery: string },
    { heading: string; description: string; unsplashQuery: string },
    { heading: string; description: string; unsplashQuery: string },
  ];
  topPersonas: Array<{
    role: "chef" | "mixologist" | "venueHost" | "curator" | "guest";
    cityHeadline: string;
    cityDescription: string;
  }>;
  tier: 1 | 2 | 3;
};
```

2. Export METROS_DATA array — copy the full array from convex/metros.ts (all 32 entries with name, displayName, cities, state, country, timezone, coordinates). This re-export allows city pages to import from a single source without depending on Convex server code.

3. Leave CITY_DATA as an empty placeholder (`export const CITY_DATA: Record<string, CityData> = {};`) — Task 2 will populate it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "^(lib/city-data|error)" | head -20</automated>
  </verify>
  <done>CityData type exported with all required fields. METROS_DATA array exported with 32 entries. CITY_DATA record exported (empty, ready for Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Populate all 32 city entries in CITY_DATA</name>
  <files>lib/city-data.ts</files>
  <action>
Populate the CITY_DATA record with all 32 city entries. All copy comes from the "City Copy Specification" / "Complete City Copy" section in tenseats-prelaunch-claude-code-prompt.md. Transcribe exactly — do not rewrite or paraphrase.

TIER 1 cities (tier: 1): chicago-il, austin-tx, atlanta-ga
TIER 2 cities (tier: 2): houston-tx, denver-co, philadelphia-pa, new-york-ny, washington-dc, charleston-sc, asheville-nc, dallas-tx, detroit-mi, charlotte-nc, columbus-oh, orlando-fl
TIER 3 cities (tier: 3): cincinnati-oh, pittsburgh-pa, tampa-fl, st-petersburg-fl, milwaukee-wi, phoenix-az, scottsdale-az, ann-arbor-mi, grand-rapids-mi, memphis-tn, indianapolis-in, buffalo-ny, birmingham-al, greenville-sc, cleveland-oh, oklahoma-city-ok, meridian-ms

For each city entry:
- slug: matches the METROS_DATA name field exactly (e.g. "chicago-il")
- displayName: matches METROS_DATA displayName (e.g. "Chicago, IL")
- state: matches METROS_DATA state
- heroHeadline, heroSubhead, metaTagline, metaDescription, sceneIntro: from prompt copy
- sceneBlocks: 3 blocks with heading and description from prompt. For unsplashQuery, derive a relevant search term for each block (e.g. "chicago logan square food", "austin food truck", "atlanta supper club"). These are query strings for potential future Unsplash integration.
- topPersonas: from prompt copy — map the shorthand (e.g. "chef (Host Chicago's next cult table)") to { role: "chef", cityHeadline: "Host Chicago's next cult table", cityDescription: "..." }. Write a 20-35 word cityDescription for each persona that relates the role to the city context.
- tier: 1, 2, or 3 per the tier classification above

IMPORTANT for Tier 2 and Tier 3 cities:
- Tier 2 cities (houston through orlando) have metaTagline, metaDescription, and topPersonas in the prompt. Some Tier 2 cities do NOT have explicit topPersonas listed — for those, assign 3 relevant personas (typically chef, guest, and one of curator/venueHost/mixologist) with city-appropriate headlines and descriptions.
- Tier 3 cities only have heroHeadline, heroSubhead, and sceneIntro in the prompt. For these, generate:
  - metaTagline: "[City]'s Curated Food Events" (same pattern as Tier 1)
  - metaDescription: ~150 char SEO description following the pattern of Tier 1 cities
  - sceneBlocks: 3 blocks derived from the sceneIntro content, splitting the city's food story into 3 distinct themes
  - topPersonas: 3 personas (chef, guest, and one other) with city-relevant headlines and descriptions

BRAND RULES (enforce in ALL copy):
- "Insider" always capital I
- "restaurant" NEVER in hero headlines or subheadlines
- Competitors (Yelp, Eventbrite, OpenTable) not named in hero copy
- All heroSubhead under 25-word hard max
- No forbidden adjectives (amazing, incredible, unique, best-in-class, world-class, revolutionary)
- Use discovery mechanic language (whisper, hidden, local, access, invitation, unlisted, off-menu)

After populating, verify word counts on all heroSubhead values — count words and confirm each is <= 25.
  </action>
  <verify>
    <automated>node -e "const d = require('./lib/city-data.ts'); console.log('entries:', Object.keys(d.CITY_DATA).length)" 2>&1 || npx tsx -e "import { CITY_DATA, METROS_DATA } from './lib/city-data'; const slugs = METROS_DATA.map(m => m.name); const missing = slugs.filter(s => !CITY_DATA[s]); console.log('Total entries:', Object.keys(CITY_DATA).length); console.log('Missing:', missing.length, missing); const over25 = Object.values(CITY_DATA).filter(c => c.heroSubhead.split(/\s+/).length > 25); console.log('Over 25-word subheads:', over25.map(c => c.slug));"</automated>
  </verify>
  <done>CITY_DATA contains exactly 32 entries, one per METROS_DATA slug. All heroSubhead values are 25 words or fewer. All entries have complete fields (heroHeadline, heroSubhead, metaTagline, metaDescription, sceneIntro, 3 sceneBlocks, topPersonas, tier).</done>
</task>

</tasks>

<verification>
- lib/city-data.ts exists and exports CityData type, METROS_DATA array (32 entries), and CITY_DATA record (32 entries)
- Every METROS_DATA slug has a corresponding CITY_DATA entry
- All heroSubhead values pass the 25-word limit
- No brand compliance violations in any copy field
- TypeScript compiles without errors
</verification>

<success_criteria>
- CityData type is complete with all required fields per spec
- METROS_DATA re-exported with all 32 metro entries
- CITY_DATA has exactly 32 entries matching all 32 metro slugs
- All copy matches the project prompt specification exactly for Tier 1 and Tier 2 cities
- Tier 3 city copy follows established patterns and brand rules
- All heroSubhead values are under 25 words
</success_criteria>

<output>
After completion, create `.planning/phases/07-city-pages/07-01-SUMMARY.md`
</output>
