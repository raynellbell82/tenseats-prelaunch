---
phase: 09-seo-deployment-copy-qa
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified: []
autonomous: false
requirements:
  - DEPL-05
  - DEPL-06
  - DEPL-07

must_haves:
  truths:
    - "npm run build exits with zero TypeScript errors"
    - "No console errors appear in the browser when visiting any page"
    - "No page at 375px viewport width has horizontal overflow"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete site builds cleanly, has no console errors, and renders correctly on mobile viewports.

Purpose: Final quality gate before deployment — confirm build health and visual integrity.
Output: Clean build, verified console output, mobile viewport confirmation.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo-deployment-copy-qa/09-01-SUMMARY.md
@.planning/phases/09-seo-deployment-copy-qa/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and fix any TypeScript or build errors</name>
  <files>Any files with TypeScript errors (unknown until build runs)</files>
  <action>
Run `npm run build` in the project root. Capture full output.

If the build succeeds with zero errors: document success.

If there are TypeScript errors:
1. Parse each error: file path, line number, error code, message
2. Fix each error in the source file. Common fixes for this codebase:
   - Missing type imports → add import
   - Implicit any → add explicit type annotation
   - Unused variables → remove or prefix with underscore
   - Module not found → check import path, fix relative/alias
3. Re-run `npm run build` after fixes to confirm clean build

If there are build warnings (not errors): document them but do not block on warnings unless they indicate real issues (e.g., "Critical dependency" warnings).

Do NOT modify next.config.ts output setting — it must remain "standalone".
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build exits with code 0, zero TypeScript errors. Any warnings documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify no console errors and no mobile viewport overflow</name>
  <files>None — human verification only</files>
  <action>
Human verifies two things in the browser after build succeeds:

1. Console error check (DEPL-06): Open Chrome DevTools Console on each key page and confirm zero JavaScript errors.
2. Mobile viewport check (DEPL-07): Set viewport to 375px width and confirm no horizontal overflow on any page.

Start the dev server: npm run dev

Console error check:
- Open Chrome DevTools (Cmd+Option+I), Console tab
- Visit: /, /join, /why-tenseats, /launch, /cities/chicago-il
- Hydration warnings are acceptable; JavaScript errors are not

Mobile viewport check:
- Open Chrome DevTools, Toggle device toolbar (Cmd+Shift+M)
- Set viewport to 375px width (iPhone SE)
- Visit: /, /join, /why-tenseats, /launch, /cities/chicago-il
- No page should allow horizontal scroll at 375px width
  </action>
  <verify>
    <automated>echo "Human verification required — no automated check available"</automated>
  </verify>
  <done>User confirms zero console errors on all key pages and zero horizontal overflow at 375px viewport on all key pages.</done>
</task>

</tasks>

<verification>
- npm run build exits 0
- User confirms no console errors on key pages
- User confirms no horizontal overflow at 375px on any page
</verification>

<success_criteria>
- Production build succeeds with zero TypeScript errors
- No JavaScript console errors on any page
- No horizontal overflow at 375px viewport width
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-deployment-copy-qa/09-03-SUMMARY.md`
</output>
