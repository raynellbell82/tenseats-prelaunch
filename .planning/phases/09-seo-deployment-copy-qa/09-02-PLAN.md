---
phase: 09-seo-deployment-copy-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - COPY-04
  - COPY-05
  - COPY-06

must_haves:
  truths:
    - "'Insider' is always capitalized with a capital I everywhere it appears"
    - "'restaurant' never appears in any hero headline or subheadline"
    - "Competitor names (Yelp, Eventbrite, OpenTable) never appear in hero copy"
    - "All subheadlines are under 25-word hard max"
    - "Forbidden adjectives (amazing, incredible, unique, best-in-class, world-class, revolutionary) appear nowhere"
    - "Discovery mechanic language (whisper, hidden, local, access, invitation, unlisted, off-menu) is used appropriately"
  artifacts: []
  key_links: []
---

<objective>
Audit all consumer-facing copy across every page component and city data for brand compliance, then fix any violations found.

Purpose: Ensure every string a visitor sees passes Tenseats brand guidelines before launch.
Output: Zero brand compliance violations across all rendered text.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all consumer-facing copy for brand compliance violations</name>
  <files>No files modified — audit only</files>
  <action>
Search the entire codebase for brand compliance violations. Check ALL of the following across every .tsx and .ts file under app/, components/, and lib/city-data.ts:

COPY-01 — "Insider" capitalization: Search case-insensitively for "insider" and verify every instance uses capital-I "Insider". Grep pattern: `insider` with -i flag, then check any lowercase matches.

COPY-02 — "restaurant" in heroes: Search for "restaurant" in any hero component file (hero-section.tsx, join-hero.tsx, city hero headlines/subheadlines in city-data.ts, why hero). Pattern: `restaurant` in hero-related files.

COPY-03 — Competitor names in hero copy: Search for "Yelp", "Eventbrite", "OpenTable" in hero component files. The WhyEconomics comparison table is allowed to name competitors — only hero headlines/subheadlines are restricted.

COPY-04 — Subheadline word count: Check all heroSubhead values in lib/city-data.ts (count words, must be <= 25). Also check subheadlines in hero-section.tsx, join-hero.tsx, why-hero.tsx.

COPY-05 — Forbidden adjectives: Search for "amazing", "incredible", "unique", "best-in-class", "world-class", "revolutionary" across all .tsx/.ts files. Case-insensitive.

COPY-06 — Discovery mechanic language: Verify that key copy uses discovery language (whisper, hidden, local, access, invitation, unlisted, off-menu). This is a positive check — confirm presence, not absence.

For each violation found, record: file path, line number, the violating text, which COPY rule it breaks, and the fix needed.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && echo "=== COPY-01: lowercase insider ===" && grep -rn '\binsider\b' app/ components/ lib/city-data.ts --include="*.tsx" --include="*.ts" | grep -v "Insider" | grep -v node_modules | grep -v ".next" || echo "PASS" && echo "=== COPY-02: restaurant in heroes ===" && grep -in "restaurant" components/launch/hero-section.tsx components/join/join-hero.tsx lib/city-data.ts 2>/dev/null | grep -i "hero\|Headline\|Subhead" || echo "PASS" && echo "=== COPY-05: forbidden adjectives ===" && grep -rni "amazing\|incredible\|unique\|best-in-class\|world-class\|revolutionary" app/ components/ lib/city-data.ts --include="*.tsx" --include="*.ts" | grep -v node_modules || echo "PASS"</automated>
  </verify>
  <done>Complete audit report of all brand compliance violations (or confirmation of zero violations). Each violation documented with file, line, text, rule, and fix.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all brand compliance violations found in audit</name>
  <files>Files identified in Task 1 audit (unknown until audit completes — files_modified in frontmatter will be empty if no fixes needed)</files>
  <action>
For each violation found in the audit:

COPY-01 fixes: Replace lowercase "insider" with "Insider" (capital I) in all consumer-facing strings. Do NOT change code identifiers, variable names, or CSS class names — only visible text strings.

COPY-02 fixes: Replace "restaurant" in hero headlines/subheadlines with appropriate discovery language (e.g., "spot", "table", "kitchen", "venue" depending on context).

COPY-03 fixes: Remove or replace competitor names in hero copy. Comparison tables (WhyEconomics) are exempt.

COPY-04 fixes: Shorten any subheadline exceeding 25 words while preserving meaning and brand voice.

COPY-05 fixes: Replace forbidden adjectives with brand-appropriate alternatives. Use discovery-oriented language instead: "hidden" instead of "unique", "curated" instead of "best-in-class", etc.

COPY-06: This is a presence check. If discovery language is absent from key headlines, add it where natural. Do NOT force it into every string.

If the audit finds zero violations, document that result and skip this task — no changes needed.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && echo "=== Re-audit COPY-01 ===" && grep -rn '\binsider\b' app/ components/ lib/city-data.ts --include="*.tsx" --include="*.ts" | grep -v "Insider" | grep -v node_modules | grep -v ".next" | grep -v "// " || echo "PASS" && echo "=== Re-audit COPY-05 ===" && grep -rni "amazing\|incredible\|unique\|best-in-class\|world-class\|revolutionary" app/ components/ lib/city-data.ts --include="*.tsx" --include="*.ts" | grep -v node_modules || echo "PASS"</automated>
  </verify>
  <done>All brand compliance violations fixed. Re-audit grep commands return zero violations for COPY-01 through COPY-05. COPY-06 discovery language confirmed present in key copy.</done>
</task>

</tasks>

<verification>
- grep -rni '\binsider\b' ... | grep -v Insider returns empty (COPY-01)
- grep -in 'restaurant' in hero files returns empty (COPY-02)
- grep -rni 'yelp\|eventbrite\|opentable' in hero files returns empty (COPY-03)
- All heroSubhead in city-data.ts have <= 25 words (COPY-04)
- grep for forbidden adjectives returns empty (COPY-05)
- Discovery language grep confirms presence in key sections (COPY-06)
</verification>

<success_criteria>
- Zero brand compliance violations across all consumer-facing text
- All fixes preserve meaning and brand voice
- Discovery mechanic language present in key headlines
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-deployment-copy-qa/09-02-SUMMARY.md`
</output>
