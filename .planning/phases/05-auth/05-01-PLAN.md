---
phase: 05-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/validations/auth.ts
  - app/(auth)/layout.tsx
  - app/(auth)/signup/page.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/verify-email/page.tsx
  - components/auth/login-form.tsx
  - components/auth/signup-wizard.tsx
  - components/auth/verify-email-form.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Visiting /signup renders a centered OTP email signup form with LandingHeader"
    - "Visiting /login renders a centered OTP email login form with LandingHeader"
    - "Submitting signup triggers Better Auth account creation and redirects to /verify-email"
    - "After OTP verification, user is redirected to / (not /feed)"
    - "Session persists across browser refresh via Better Auth cookies"
  artifacts:
    - path: "lib/validations/auth.ts"
      provides: "Zod schemas for login, signup, password reset forms"
      contains: "loginSchema"
    - path: "app/(auth)/layout.tsx"
      provides: "Centered auth layout with LandingHeader"
      min_lines: 10
    - path: "app/(auth)/signup/page.tsx"
      provides: "Signup page rendering SignupWizard"
      contains: "SignupWizard"
    - path: "app/(auth)/login/page.tsx"
      provides: "Login page rendering LoginForm"
      contains: "LoginForm"
    - path: "app/(auth)/verify-email/page.tsx"
      provides: "OTP verification page rendering VerifyEmailForm"
      contains: "VerifyEmailForm"
  key_links:
    - from: "app/(auth)/signup/page.tsx"
      to: "components/auth/signup-wizard.tsx"
      via: "import SignupWizard"
      pattern: "import.*SignupWizard"
    - from: "app/(auth)/login/page.tsx"
      to: "components/auth/login-form.tsx"
      via: "import LoginForm"
      pattern: "import.*LoginForm"
    - from: "components/auth/login-form.tsx"
      to: "lib/validations/auth.ts"
      via: "import loginSchema"
      pattern: "import.*loginSchema.*validations/auth"
    - from: "components/auth/verify-email-form.tsx"
      to: "lib/auth-client.ts"
      via: "authClient.emailOtp.verifyEmail"
      pattern: "authClient\\.emailOtp\\.verifyEmail"
    - from: "components/auth/signup-wizard.tsx"
      to: "lib/auth-client.ts"
      via: "signUp.email"
      pattern: "signUp\\.email"
---

<objective>
Create the auth pages (/signup, /login, /verify-email) with a shared centered layout and LandingHeader, ensuring all auth components point to pre-launch routes (/ instead of /feed).

Purpose: Visitors can create accounts and log in via OTP email on the pre-launch site, with sessions persisting across the shared Convex backend.
Output: 3 auth route pages, 1 auth layout, validation schema, patched redirect targets.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/layout.tsx
@app/page.tsx
@lib/auth-client.ts
@lib/auth-server.ts
@app/api/auth/[...all]/route.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/auth-client.ts:
```typescript
export const authClient = createAuthClient({
  plugins: [convexClient(), emailOTPClient()],
});
export const { signIn, signUp, signOut, useSession, getSession } = authClient;
```

From lib/auth-server.ts:
```typescript
export const { handler, preloadAuthQuery, isAuthenticated, getToken, fetchAuthQuery, fetchAuthMutation, fetchAuthAction } = convexBetterAuthNextJs({ ... });
```

From components/auth/login-form.tsx:
```typescript
import { loginSchema, LoginFormData } from "@/lib/validations/auth";
// Redirects to /feed on success — MUST change to /
// Redirects to /verify-email on unverified — keep as-is
```

From components/auth/signup-wizard.tsx:
```typescript
import { signupSchema, type SignupFormData } from "@/lib/validations/auth";
// 4-step wizard: Credentials, Profile, Username, Location
// Redirects to /verify-email on success — keep as-is
```

From components/auth/verify-email-form.tsx:
```typescript
// Redirects to /feed on success — MUST change to /
```

LandingHeader import path:
```typescript
import { LandingHeader } from "@/components/landing/landing-header";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy validation schema and create auth route group layout</name>
  <files>lib/validations/auth.ts, app/(auth)/layout.tsx</files>
  <action>
1. Copy `lib/validations/auth.ts` from the source repo at `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/lib/validations/auth.ts` into `lib/validations/auth.ts`. Copy the file unchanged — it contains Zod schemas for login, signup, profile, username, metro, forgot-password, and reset-password forms. Create the `lib/validations/` directory first.

2. Create `app/(auth)/layout.tsx` as a Server Component with:
   - Import LandingHeader from `@/components/landing/landing-header`
   - NO SetupProvider check (removed per Phase 2 decision)
   - NO Navbar import (pre-launch uses LandingHeader, not Navbar)
   - Centered layout: `min-h-screen flex flex-col items-center justify-center px-4 py-12 bg-background`
   - Content wrapper: `w-full max-w-md`
   - Tenseats text logo link to `/` above the form: `<Link href="/"><span className="text-2xl font-bold text-foreground">Tenseats</span></Link>` with `block text-center mb-8`
   - Render `{children}` inside the content wrapper
   - Place `<LandingHeader />` before the centered div (outside it, at the top)

LandingHeader is already a Client Component so it can be imported into this Server Component without issues.
  </action>
  <verify>
    <automated>ls lib/validations/auth.ts app/\(auth\)/layout.tsx && grep -q "loginSchema" lib/validations/auth.ts && grep -q "LandingHeader" app/\(auth\)/layout.tsx</automated>
  </verify>
  <done>lib/validations/auth.ts exists with all Zod schemas. app/(auth)/layout.tsx exists as a Server Component rendering LandingHeader + centered content area.</done>
</task>

<task type="auto">
  <name>Task 2: Create signup, login, and verify-email route pages</name>
  <files>app/(auth)/signup/page.tsx, app/(auth)/login/page.tsx, app/(auth)/verify-email/page.tsx</files>
  <action>
1. Create `app/(auth)/signup/page.tsx` as a Server Component (no "use client"):
   - Import SignupWizard from `@/components/auth/signup-wizard`
   - Render heading "Create your account", subtext "Join Tenseats to discover local events"
   - Render `<SignupWizard />`
   - Below wizard: "Already have an account?" link to `/login` with `text-primary hover:underline`
   - Copy structure exactly from source: `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/app/(auth)/signup/page.tsx`

2. Create `app/(auth)/login/page.tsx` as an async Server Component:
   - Import LoginForm from `@/components/auth/login-form`
   - Accept `searchParams` prop as `Promise<{ reset?: string }>`
   - If `searchParams.reset === "success"`, show green success banner "Password reset successful!"
   - Render heading "Welcome back", subtext "Sign in to your account"
   - Render `<LoginForm />`
   - Below form: "Don't have an account?" link to `/signup`
   - Wrap content in Suspense with loading fallback
   - Copy structure from source: `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/app/(auth)/login/page.tsx`

3. Create `app/(auth)/verify-email/page.tsx` as a Client Component ("use client"):
   - Import VerifyEmailForm from `@/components/auth/verify-email-form`
   - Import useSession from `@/lib/auth-client`
   - Import useRouter, useSearchParams from next/navigation
   - On mount: if no session redirect to `/login`, if already verified redirect to `/` (NOT /feed)
   - Get email from session > searchParams > sessionStorage fallback
   - Get initialCode from searchParams for magic link auto-fill
   - Show Loader2 spinner while session is pending
   - Render `<VerifyEmailForm email={email} initialCode={initialCode} />`
   - Copy structure from source but change `/feed` redirect to `/`
  </action>
  <verify>
    <automated>ls app/\(auth\)/signup/page.tsx app/\(auth\)/login/page.tsx app/\(auth\)/verify-email/page.tsx && grep -q "SignupWizard" app/\(auth\)/signup/page.tsx && grep -q "LoginForm" app/\(auth\)/login/page.tsx && grep -q "VerifyEmailForm" app/\(auth\)/verify-email/page.tsx</automated>
  </verify>
  <done>/signup renders SignupWizard. /login renders LoginForm. /verify-email renders VerifyEmailForm. All three use the (auth) route group layout.</done>
</task>

<task type="auto">
  <name>Task 3: Patch auth component redirects from /feed to / for pre-launch</name>
  <files>components/auth/login-form.tsx, components/auth/verify-email-form.tsx</files>
  <action>
1. In `components/auth/login-form.tsx`:
   - Change `router.push("/feed")` (line 124) to `router.push("/")`
   - This is the post-login redirect after successful authentication + profile creation

2. In `components/auth/verify-email-form.tsx`:
   - Change `router.push("/feed")` (line 179) inside the `submitOtp` success handler to `router.push("/")`
   - This is the post-verification redirect after email OTP is confirmed

Do NOT change `/verify-email` redirects in either file — those are internal auth flow redirects that must remain.
Do NOT change `/signup` or `/login` links — those are cross-auth-page navigation and must remain.

These are the only two files that redirect to `/feed`. The signup wizard already redirects to `/verify-email` which is correct.
  </action>
  <verify>
    <automated>! grep -r '"/feed"' components/auth/ && echo "No /feed redirects remain"</automated>
  </verify>
  <done>All auth component redirects point to / (pre-launch homepage) instead of /feed (main app). Internal auth flow redirects (/verify-email, /signup, /login) unchanged.</done>
</task>

</tasks>

<verification>
1. `ls lib/validations/auth.ts app/\(auth\)/layout.tsx app/\(auth\)/signup/page.tsx app/\(auth\)/login/page.tsx app/\(auth\)/verify-email/page.tsx` — all 5 files exist
2. `grep -r '"/feed"' components/auth/ app/\(auth\)/` — returns no matches (all redirects patched)
3. `grep -q "LandingHeader" app/\(auth\)/layout.tsx` — auth layout uses LandingHeader
4. `npx next build 2>&1 | tail -5` — build succeeds (or at least no TypeScript errors in auth files)
</verification>

<success_criteria>
- /signup route exists and renders centered SignupWizard with LandingHeader
- /login route exists and renders centered LoginForm with LandingHeader
- /verify-email route exists and renders VerifyEmailForm
- All post-auth redirects point to / (not /feed)
- Validation schemas exist at lib/validations/auth.ts
- Build compiles without TypeScript errors in auth files
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth/05-01-SUMMARY.md`
</output>
