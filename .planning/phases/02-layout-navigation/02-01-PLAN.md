---
phase: 02-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/providers.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - LAYO-01
  - LAYO-02
  - LAYO-06

must_haves:
  truths:
    - "Every page is wrapped in ThemeProvider (dark default) and ConvexClientProvider"
    - "Footer renders on every page"
    - "Toaster is available for toast notifications site-wide"
    - "Dark theme is applied by default — no flash of light theme on load"
    - "Geist font family is loaded and applied"
  artifacts:
    - path: "app/providers.tsx"
      provides: "ThemeProvider + ConvexClientProvider wrapper, NO SetupProvider"
      exports: ["Providers"]
    - path: "app/layout.tsx"
      provides: "Root layout with fonts, providers, footer, toaster"
      contains: "Providers"
  key_links:
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "import { Providers }"
      pattern: "import.*Providers.*from.*providers"
    - from: "app/layout.tsx"
      to: "components/shared/footer"
      via: "import { Footer }"
      pattern: "import.*Footer.*from.*shared/footer"
    - from: "app/layout.tsx"
      to: "components/ui/sonner"
      via: "import { Toaster }"
      pattern: "import.*Toaster.*from.*ui/sonner"
    - from: "app/providers.tsx"
      to: "lib/convex.tsx"
      via: "import { ConvexClientProvider }"
      pattern: "ConvexClientProvider"
---

<objective>
Create the Providers component and root layout that wrap every page in the pre-launch site with dark-theme-default ThemeProvider, ConvexClientProvider, Footer, and Toaster.

Purpose: Establishes the app shell so all subsequent pages render inside the correct theme, data, and notification context.
Output: app/providers.tsx and app/layout.tsx — the two foundational files every route depends on.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing components the executor needs -->

From lib/convex.tsx:
```typescript
export function ConvexClientProvider({
  children,
  initialToken,
}: PropsWithChildren<{ initialToken?: string | null }>)
```

From components/shared/footer/index.tsx:
```typescript
export function Footer()
```

From components/ui/sonner.tsx:
```typescript
// shadcn Toaster wrapper — standard import
export { Toaster }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Providers and root layout</name>
  <files>app/providers.tsx, app/layout.tsx</files>
  <action>
**app/providers.tsx** — Copy from source at `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/app/providers.tsx` then modify:
1. Remove the `SetupProvider` import and wrapping entirely (per PROJECT.md: "remove SetupProvider")
2. Change ThemeProvider `defaultTheme` from `"system"` to `"dark"` (LAYO-01 requires dark default)
3. Keep `enableSystem` and `disableTransitionOnChange`
4. Keep `attribute="class"` (CSS vars use `.dark` class)
5. Keep ConvexClientProvider wrapping children inside ThemeProvider

Result should be:
```
"use client";
ThemeProvider (attribute="class", defaultTheme="dark", enableSystem, disableTransitionOnChange)
  > ConvexClientProvider
    > {children}
```

**app/layout.tsx** — Rewrite the existing boilerplate to match the source repo pattern:
1. Import Geist and Geist_Mono from `next/font/google`, configure with `variable` and `subsets: ["latin"]`
2. Import `Providers` from `./providers`
3. Import `Footer` from `@/components/shared/footer`
4. Import `Toaster` from `@/components/ui/sonner`
5. Import `./globals.css`
6. Export `metadata` with title "Tenseats - Discover Events Worth Attending" and description "The food-focused social marketplace launching in 32 US cities. Join the founding membership."
7. Export `viewport` with `width: "device-width"`, `initialScale: 1`, `viewportFit: "cover"`
8. In the layout: `<html lang="en" suppressHydrationWarning>` (suppressHydrationWarning required by next-themes)
9. `<body>` with font variables and `antialiased min-h-screen flex flex-col` classes
10. Body contents: `<Providers>` wrapping `<main className="flex-1">{children}</main>`, `<Footer />`, `<Toaster />`
11. Do NOT import DynamicTitle (not relevant for pre-launch)
12. Do NOT import or add LandingHeader here — it will be added per-page in later phases (header is not in the source root layout either)
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit app/providers.tsx app/layout.tsx 2>&1 | head -30</automated>
  </verify>
  <done>
    - app/providers.tsx exports Providers with ThemeProvider (dark default) + ConvexClientProvider, no SetupProvider
    - app/layout.tsx loads Geist fonts, wraps children in Providers, renders Footer and Toaster
    - suppressHydrationWarning on html element prevents theme flash
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `grep -c "SetupProvider" app/providers.tsx` returns 0 (SetupProvider removed)
2. `grep "defaultTheme" app/providers.tsx` shows `"dark"`
3. `grep "Footer" app/layout.tsx` confirms Footer import and usage
4. `grep "Toaster" app/layout.tsx` confirms Toaster import and usage
5. `grep "suppressHydrationWarning" app/layout.tsx` confirms theme flash prevention
</verification>

<success_criteria>
- Providers component wraps ThemeProvider (dark default) around ConvexClientProvider with no SetupProvider
- Root layout loads Geist fonts, applies font variables to body, includes Footer and Toaster
- Dark theme class is applied by default on first load
- Footer renders at the bottom of every page
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-01-SUMMARY.md`
</output>
