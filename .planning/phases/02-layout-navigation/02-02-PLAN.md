---
phase: 02-layout-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/landing/landing-header.tsx
autonomous: false
requirements:
  - LAYO-03
  - LAYO-04
  - LAYO-05

must_haves:
  truths:
    - "Header shows nav links to /join, /why-tenseats, /cities on desktop"
    - "Mobile hamburger menu opens and closes at small viewports"
    - "Logo loads from Convex platformSettings (dark logo visible in dark mode)"
    - "Dark/light theme toggle button works in the header"
  artifacts:
    - path: "components/landing/landing-header.tsx"
      provides: "LandingHeader with logo, nav, theme toggle, mobile menu"
      exports: ["LandingHeader"]
      min_lines: 60
  key_links:
    - from: "components/landing/landing-header.tsx"
      to: "convex/platformSettings"
      via: "useQuery(api.platformSettings.getLogoUrl)"
      pattern: "useQuery.*platformSettings"
    - from: "components/landing/landing-header.tsx"
      to: "next-themes"
      via: "useTheme()"
      pattern: "useTheme"
---

<objective>
Create the LandingHeader component with full navigation, logo from Convex, theme toggle, and mobile hamburger menu.

Purpose: Every page in the pre-launch site needs a consistent header with navigation to /join, /why-tenseats, and /cities plus mobile responsiveness.
Output: components/landing/landing-header.tsx ready for per-page inclusion.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Source landing-header to copy and modify -->
<!-- Location: /Users/tenseats/Documents/dev/Tenseats-marketplace-platform/components/landing/landing-header.tsx -->

From convex/platformSettings.ts (via Convex queries):
```typescript
api.platformSettings.getLogoUrl     // returns string | null
api.platformSettings.getDarkLogoUrl // returns string | null
api.platformSettings.getPlatformName // returns string | null
```

From lib/utils.ts:
```typescript
export const fixUploadUrl: (url: string) => string;
```

From components/ui/button.tsx:
```typescript
export const Button: React.FC<ButtonProps>; // variant: "ghost" | "default" | ...
```

From components/ui/sheet.tsx:
```typescript
export { Sheet, SheetTrigger, SheetContent, SheetClose }; // shadcn sheet for mobile menu
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LandingHeader with nav links and mobile menu</name>
  <files>components/landing/landing-header.tsx</files>
  <action>
Copy the source landing-header from `/Users/tenseats/Documents/dev/Tenseats-marketplace-platform/components/landing/landing-header.tsx` then modify per PROJECT.md ("Modify only landing-header.tsx — nav links"):

**Desktop nav changes (the `hidden md:flex` div):**
1. Keep existing "Join" link to /join (amber-400 styling)
2. Replace "Log in" link with three nav links:
   - "Why Tenseats" linking to /why-tenseats (text-muted-foreground hover:text-foreground)
   - "Cities" linking to /cities (text-muted-foreground hover:text-foreground)
   - "Log in" linking to /login (text-muted-foreground hover:text-foreground) — keep this last
3. Keep the theme toggle button exactly as-is (Sun/Moon icons)

**Add mobile hamburger menu (new):**
The source header has NO mobile menu — only `hidden md:flex` for desktop nav. Add a mobile menu using shadcn Sheet:

1. Import `Sheet, SheetTrigger, SheetContent, SheetClose` from `@/components/ui/sheet`
2. Import `Menu` icon from `lucide-react`
3. Add a `md:hidden` hamburger button (Menu icon) wrapped in SheetTrigger
4. SheetContent (side="right") containing:
   - Nav links as full-width buttons/links: Join (/join), Why Tenseats (/why-tenseats), Cities (/cities), Log in (/login)
   - Each link wrapped in SheetClose so the menu closes on navigation
5. Use `useState` for sheet open state so links can close it programmatically
6. Style: dark bg matching theme, links stacked vertically with padding

**Keep unchanged:**
- Logo section (getLogoUrl, getDarkLogoUrl, platformName) — already correct for LAYO-04
- Theme toggle — already correct for LAYO-05
- Sticky header with backdrop blur
- All existing imports and Convex queries
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit components/landing/landing-header.tsx 2>&1 | head -30</automated>
  </verify>
  <done>
    - LandingHeader exports with nav links to /join, /why-tenseats, /cities, /login on desktop
    - Mobile hamburger menu opens Sheet with same nav links, closes on link click
    - Logo loads from Convex platformSettings with dark mode variant
    - Theme toggle button switches dark/light
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify layout shell and header</name>
  <action>
Human verifies the complete layout shell: root layout with dark theme providers, footer, toaster, and LandingHeader with navigation and mobile menu.
  </action>
  <what-built>Complete layout shell: root layout with dark theme providers, footer, toaster, and LandingHeader with navigation and mobile menu</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the tenseats-prelaunch directory
    2. Visit http://localhost:3001
    3. Verify: dark theme applied by default (dark background, light text)
    4. Verify: Footer visible at the bottom with copyright and links
    5. Verify: No flash of light theme on page load
    6. Open the page.tsx file temporarily and add `import { LandingHeader } from "@/components/landing/landing-header"` and `<LandingHeader />` before the existing content, then:
       - Verify: Logo appears in header (or text fallback "Tenseats")
       - Verify: Desktop nav shows Join, Why Tenseats, Cities, Log in links
       - Verify: Theme toggle (sun/moon) switches between dark and light
       - Resize browser to 375px width: verify hamburger menu icon appears, clicking it opens a slide-out panel with nav links
    7. Revert page.tsx changes after testing (LandingHeader will be added per-page in Phase 3+)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep "why-tenseats" components/landing/landing-header.tsx` confirms /why-tenseats link
2. `grep "cities" components/landing/landing-header.tsx` confirms /cities link
3. `grep "Sheet" components/landing/landing-header.tsx` confirms mobile menu Sheet usage
4. `grep "Menu" components/landing/landing-header.tsx` confirms hamburger icon
5. `grep "md:hidden" components/landing/landing-header.tsx` confirms mobile-only trigger
</verification>

<success_criteria>
- LandingHeader renders with logo, 4 nav links on desktop, theme toggle
- Mobile hamburger menu works at 375px with slide-out navigation
- Dark theme is applied by default across the site
- Footer and Toaster render on all pages
- All 6 LAYO requirements are addressed across plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-02-SUMMARY.md`
</output>
