---
phase: 04-join-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/join/page.tsx
autonomous: false
requirements:
  - JOIN-01
  - JOIN-02
  - JOIN-03
  - JOIN-04
  - JOIN-05
  - JOIN-06

must_haves:
  truths:
    - "/join renders all 5 sections in order: hero, food-as-language, free seat, reserve spot, CTA footer"
    - "JoinHero displays 'Find where locals only whisper.' headline"
    - "ReserveSpotSection renders tier cards and countdown (when enabled in Convex config)"
    - "Page is readable and usable at 375px viewport width"
  artifacts:
    - path: "app/join/page.tsx"
      provides: "Join page route assembling all 5 join sections with LandingHeader"
      min_lines: 10
  key_links:
    - from: "app/join/page.tsx"
      to: "components/join/join-hero.tsx"
      via: "import { JoinHero }"
      pattern: "import.*JoinHero.*from.*components/join"
    - from: "app/join/page.tsx"
      to: "components/join/food-as-language-section.tsx"
      via: "import { FoodAsLanguageSection }"
      pattern: "import.*FoodAsLanguageSection.*from.*components/join"
    - from: "app/join/page.tsx"
      to: "components/join/free-seat-section.tsx"
      via: "import { FreeSeatSection }"
      pattern: "import.*FreeSeatSection.*from.*components/join"
    - from: "app/join/page.tsx"
      to: "components/join/reserve-spot-section.tsx"
      via: "import { ReserveSpotSection }"
      pattern: "import.*ReserveSpotSection.*from.*components/join"
    - from: "app/join/page.tsx"
      to: "components/join/join-cta-footer.tsx"
      via: "import { JoinCtaFooter }"
      pattern: "import.*JoinCtaFooter.*from.*components/join"
    - from: "app/join/page.tsx"
      to: "components/landing/landing-header.tsx"
      via: "import { LandingHeader }"
      pattern: "import.*LandingHeader.*from.*components/landing"
---

<objective>
Create the /join route page that assembles all 5 existing join components with the LandingHeader.

Purpose: The join page is the primary conversion page where visitors read the Tenseats value proposition, explore roles, see membership tiers, and navigate toward signup or the launch flow.
Output: A working /join route rendering JoinHero, FoodAsLanguageSection, FreeSeatSection, ReserveSpotSection, and JoinCtaFooter in sequence.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/page.tsx
</context>

<interfaces>
<!-- Key exports the executor needs. All 5 join components and LandingHeader are already built. -->

From components/join/join-hero.tsx:
```typescript
export function JoinHero(): JSX.Element
// "use client" — uses framer-motion, renders "Find where locals only whisper." headline
```

From components/join/food-as-language-section.tsx:
```typescript
export function FoodAsLanguageSection(): JSX.Element
// "use client" — uses framer-motion + Next Image, 3 food story cards
```

From components/join/free-seat-section.tsx:
```typescript
export function FreeSeatSection(): JSX.Element
// "use client" — uses useState, framer-motion, 5 role tabs (Chef, Mixologist, Creator, Venue Host, Guest)
```

From components/join/reserve-spot-section.tsx:
```typescript
export function ReserveSpotSection(): JSX.Element
// "use client" — uses useQuery(api.launch.queue.getLaunchConfig), renders Early Bird and Founding tier cards
// Links to /launch for checkout flow
```

From components/join/join-cta-footer.tsx:
```typescript
export function JoinCtaFooter(): JSX.Element
// "use client" — "Your city is waiting. Take your seat." with Join Tenseats CTA
```

From components/landing/landing-header.tsx:
```typescript
export function LandingHeader(): JSX.Element
// "use client" — nav with logo, links, theme toggle, mobile menu
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create /join route page</name>
  <files>app/join/page.tsx</files>
  <action>
Create app/join/page.tsx following the exact pattern from app/page.tsx (Server Component that imports and renders Client Components).

The page must:
1. Import LandingHeader from @/components/landing/landing-header
2. Import all 5 join components from @/components/join/:
   - JoinHero
   - FoodAsLanguageSection
   - FreeSeatSection
   - ReserveSpotSection
   - JoinCtaFooter
3. Export default function JoinPage that renders them in a fragment (<>...</>)
4. Section order: LandingHeader, JoinHero, FoodAsLanguageSection, FreeSeatSection, ReserveSpotSection, JoinCtaFooter
5. Do NOT add "use client" at the page level — this is a Server Component rendering Client Components (same pattern as homepage)
6. Do NOT wrap sections in extra divs or add custom styles — each component manages its own layout
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>app/join/page.tsx exists, imports all 5 join components + LandingHeader, build succeeds with no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify join page renders correctly</name>
  <files>app/join/page.tsx</files>
  <action>Human verifies the join page visually at desktop and mobile viewports.</action>
  <what-built>Join page at /join with all 5 sections: hero, food-as-language, free seat roles, reserve spot tiers, and CTA footer</what-built>
  <how-to-verify>
    1. Run `npm run dev` on port 3001 if not already running
    2. Visit http://localhost:3001/join
    3. Verify JoinHero renders with "Find where locals only whisper." headline
    4. Scroll down — verify FoodAsLanguageSection shows 3 image cards with food story copy
    5. Scroll to FreeSeatSection — click through all 5 role tabs (Chef, Mixologist, Creator, Venue Host, Guest) and verify content changes
    6. Scroll to ReserveSpotSection — verify Early Bird and Founding tier cards appear
    7. Scroll to JoinCtaFooter — verify "Your city is waiting. Take your seat." and Join Tenseats button
    8. Resize browser to 375px width — verify all sections are readable without horizontal overflow
    9. Check that LandingHeader appears at top with nav links
  </how-to-verify>
  <verify>Human confirms all 5 sections render correctly at desktop and 375px mobile</verify>
  <done>User approves the /join page visual and functional state</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` completes without TypeScript errors
- /join route resolves (no 404)
- All 5 join components render in correct order
- Page works at mobile viewport (375px)
</verification>

<success_criteria>
- /join renders all 5 sections: hero, food-as-language, free seat, reserve spot, CTA footer
- JoinHero displays "Find where locals only whisper." headline
- ReserveSpotSection shows tier cards (Early Bird, Founding) with "Reserve [Tier] Seat" CTAs linking to /launch
- Page is usable at 375px viewport width
- No TypeScript build errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-join-page/04-01-SUMMARY.md`
</output>
