---
phase: 08.1-cities-globe
verified: 2026-03-13T11:17:51Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 08.1: Cities Globe Verification Report

**Phase Goal:** Visitors on the homepage can see all 32 Tenseats cities on an interactive 3D globe and navigate to any city page via region-filtered clickable pills
**Verified:** 2026-03-13T11:17:51Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                       | Status     | Evidence                                                                                             |
| --- | --------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------- |
| 1   | Homepage shows a rotating 3D globe below the hero with 32 orange city markers | ✓ VERIFIED | `cities-globe.tsx` line 80: `createGlobe` called with `markerColor: [0.976, 0.451, 0.086]` and all 32 METROS_DATA markers mapped at line 48–51; `app/page.tsx` renders `<CitiesGlobe />` after `<HeroSection />` |
| 2   | Region tabs (South, Midwest, Northeast, West) filter the city pill list     | ✓ VERIFIED | `REGIONS` array drives tab render (line 254); `getCitiesByRegion(activeRegion)` filters pills (line 200); `useState<Region>("South")` as default (line 44) |
| 3   | Hovering a city pill smoothly rotates the globe to that city                | ✓ VERIFIED | `handlePillMouseEnter` (line 180) debounces 150ms then sets `focusRef.current` with lerp-target coords; `onRender` lerps toward focus at factor 0.05 (line 97–112) |
| 4   | Clicking a city pill navigates to /cities/[slug]                            | ✓ VERIFIED | `<Link href={"/cities/${metro.name}"}` at line 284–285; all 32 METROS_DATA slugs match `/cities/[slug]` routes confirmed by build output |
| 5   | Globe auto-rotates when idle and starts focused on the continental US       | ✓ VERIFIED | `US_CENTER_PHI` set at line 22; `autoRotateRef.current = true` default; `phi += 0.003` on each frame when idle (line 107); 2s idle timer re-enables auto-rotate on mouse-leave (line 195–197) |
| 6   | If WebGL fails, tabs and pills still render and function                    | ✓ VERIFIED | `{!globeError && (...canvas...)}` conditional at line 229; three `setGlobeError(true)` catch paths (lines 118, 121, 124); tabs and pills are outside the conditional and always render |
| 7   | Globe canvas resizes responsively: 500px desktop, 400px tablet, 300px mobile | ✓ VERIFIED | `getCanvasSize()` at lines 25–30 returns 500/400/300 based on matchMedia; canvas CSS `width/height` applied at lines 239–240; globe recreated on breakpoint change via `handleResize` (lines 154–158) |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                                        | Expected                                     | Status     | Details                                                                     |
| ----------------------------------------------- | -------------------------------------------- | ---------- | --------------------------------------------------------------------------- |
| `lib/city-regions.ts`                           | Region mapping for all 32 cities             | ✓ VERIFIED | 77 lines; exports `Region`, `REGIONS`, `CITY_REGIONS` (32 slugs), `getCitiesByRegion`; all 32 METROS_DATA slugs mapped (diff confirmed zero gaps) |
| `components/landing/cities-globe.tsx`           | CitiesGlobe client component                 | ✓ VERIFIED | 300 lines; `"use client"`; full implementation including globe, tabs, pills, WebGL fallback, IntersectionObserver, responsive sizing, ARIA |
| `components/landing/cities-globe-loader.tsx`    | Client wrapper for next/dynamic ssr:false    | ✓ VERIFIED | Thin `"use client"` loader; exports named `CitiesGlobe` via `dynamic(..., { ssr: false })` |
| `app/page.tsx`                                  | Homepage rendering CitiesGlobe below HeroSection | ✓ VERIFIED | Imports from `cities-globe-loader`; renders `<CitiesGlobe />` after `<HeroSection />` |

Note: The plan listed 3 artifacts but implementation correctly added `cities-globe-loader.tsx` as a fourth artifact, required by Next.js 16 Turbopack's constraint that `next/dynamic ssr:false` must live in a Client Component. This is a valid deviation that the SUMMARY documented.

### Key Link Verification

| From                                    | To                          | Via                                      | Status     | Details                                                             |
| --------------------------------------- | --------------------------- | ---------------------------------------- | ---------- | ------------------------------------------------------------------- |
| `components/landing/cities-globe.tsx`   | `lib/city-data.ts`          | import METROS_DATA                       | ✓ WIRED    | Line 6: `import { METROS_DATA } from "@/lib/city-data";` used at lines 48 and 73 |
| `components/landing/cities-globe.tsx`   | `lib/city-regions.ts`       | import for tab filtering                 | ✓ WIRED    | Line 7: imports `REGIONS`, `getCitiesByRegion`, `Region`; used at lines 44, 200, 254 (Note: plan pattern expected `CITY_REGIONS` import but `getCitiesByRegion` is the correct abstraction — functionally equivalent) |
| `components/landing/cities-globe.tsx`   | `/cities/[slug]`            | Next.js Link on each city pill           | ✓ WIRED    | Line 284–285: `href={"/cities/${metro.name}"}` inside `<Link>` component |
| `app/page.tsx`                          | `components/landing/cities-globe.tsx` | dynamic import with ssr: false (via loader) | ✓ WIRED | `cities-globe-loader.tsx` does `dynamic(..., { ssr: false })`; `app/page.tsx` imports named `CitiesGlobe` from loader |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                   | Status      | Evidence                                                                                      |
| ----------- | ----------- | ----------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------- |
| GLOB-01     | 08.1-01     | Interactive 3D globe (cobe) on homepage showing 32 city markers with orange glowing dots | ✓ SATISFIED | cobe installed (package.json line 75); `createGlobe` in `cities-globe.tsx`; 32 markers from METROS_DATA; `markerColor: [0.976, 0.451, 0.086]` (orange) |
| GLOB-02     | 08.1-01     | Region-tabbed pill system (South, Midwest, Northeast, West) filtering city pills | ✓ SATISFIED | `REGIONS` drives 4-tab render; `setActiveRegion` on click; `getCitiesByRegion(activeRegion)` filters pills |
| GLOB-03     | 08.1-01     | Pill hover rotates globe to city location; pill click navigates to /cities/[slug] | ✓ SATISFIED | `handlePillMouseEnter` sets `focusRef` for globe rotation; `<Link href="/cities/...">` on each pill |
| GLOB-04     | 08.1-01     | Globe auto-rotates when idle, starts focused on continental US                | ✓ SATISFIED | `US_CENTER_PHI` initial position; `phi += 0.003` auto-rotation; 2s idle timer re-enables |
| GLOB-05     | 08.1-01     | WebGL fallback — tabs + pills remain functional if globe fails to render       | ✓ SATISFIED | `globeError` state; canvas conditionally rendered `{!globeError && ...}`; 3 catch paths; tabs/pills outside conditional |
| GLOB-06     | 08.1-01     | Accessible: ARIA roles on tabs, keyboard navigable pills, prefers-reduced-motion | ✓ SATISFIED | `role="tablist"`, `role="tab"`, `aria-selected`, `role="img"`, `aria-hidden="true"` all present; `prefers-reduced-motion` checked at line 59; `<Link>` pills are keyboard-navigable natively |
| GLOB-07     | 08.1-01     | Responsive: 500px desktop, 400px tablet, 300px mobile canvas sizing           | ✓ SATISFIED | `getCanvasSize()` returns 500/400/300 based on `matchMedia`; globe recreated on breakpoint transition |

### Anti-Patterns Found

None detected. No TODO/FIXME/PLACEHOLDER comments, no stub returns, no empty handlers, no console.log-only implementations found in any phase files.

### Human Verification Required

#### 1. Globe visual rendering on homepage

**Test:** Open the homepage in a browser with WebGL enabled. Scroll below the hero.
**Expected:** A dark 3D globe appears with 32 small orange glowing dots. The globe rotates slowly.
**Why human:** Cannot verify WebGL canvas rendering output programmatically.

#### 2. Pill hover globe rotation

**Test:** Hover over a city pill (e.g., "Chicago, IL" in Midwest tab). Wait ~150ms.
**Expected:** The globe smoothly rotates to center the Chicago marker.
**Why human:** Lerp animation behavior requires visual inspection to confirm smoothness and correctness.

#### 3. Region tab switching animation

**Test:** Click between South, Midwest, Northeast, and West tabs.
**Expected:** City pills animate in/out smoothly via Framer Motion AnimatePresence layout transitions.
**Why human:** Framer Motion animation quality is a visual judgment.

#### 4. Mobile scroll tabs

**Test:** View on a mobile viewport (<768px). Check the region tabs row.
**Expected:** Tabs are horizontally scrollable if they overflow, no clipping.
**Why human:** Requires device/viewport testing.

#### 5. prefers-reduced-motion behavior

**Test:** Enable "Reduce Motion" in OS accessibility settings. Load homepage.
**Expected:** Globe does not auto-rotate; pill hover snaps globe position instantly.
**Why human:** Requires OS-level accessibility setting to test.

### Gaps Summary

No gaps. All 7 observable truths verified, all 4 artifacts are substantive and wired, all 4 key links are connected, all 7 GLOB requirements are satisfied. Production build passes cleanly (`npm run build` — 49 static pages, 0 errors).

One plan deviation was correctly handled by the executor: the `cities-globe-loader.tsx` client wrapper was added to satisfy a Next.js 16 Turbopack constraint. This is an additive change that improves the architecture and does not break any requirement. The PLAN's key_link pattern `import.*CITY_REGIONS.*city-regions` was technically not matched (the component imports `getCitiesByRegion` instead), but the functional intent — filtering city pills by region using city-regions.ts — is fully satisfied and is better API design.

---

_Verified: 2026-03-13T11:17:51Z_
_Verifier: Claude (gsd-verifier)_
