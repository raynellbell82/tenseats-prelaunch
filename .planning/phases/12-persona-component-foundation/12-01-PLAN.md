---
phase: 12-persona-component-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/city-data.ts
  - components/icons/persona-icons.tsx
  - components/shared/persona-card.tsx
  - components/cities/city-personas.tsx
  - components/why/why-personas.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, DATA-02]

must_haves:
  truths:
    - "Any city page displays 6 persona cards in a 3x2 desktop / 2x3 tablet / 1x6 mobile grid"
    - "Facilitator card appears with distinct icon and correct role title alongside existing 5 roles"
    - "Card design shows icon at top, uppercase role badge, bold headline, description -- dark card style"
    - "Why-tenseats page renders same card design with shared PersonaCard component and custom icons"
    - "No per-card CTA buttons -- single section-level Find your seat CTA below grid"
    - "CityData type accepts facilitator in role union without type errors"
  artifacts:
    - path: "lib/city-data.ts"
      provides: "Updated CityData type with facilitator in role union"
      contains: "facilitator"
    - path: "components/icons/persona-icons.tsx"
      provides: "6 custom monoline SVG icon components"
      exports: ["GuestIcon", "ChefIcon", "MixologistIcon", "CuratorIcon", "VenueHostIcon", "FacilitatorIcon"]
    - path: "components/shared/persona-card.tsx"
      provides: "Shared PersonaCard component used by both city-personas and why-personas"
      exports: ["PersonaCard"]
    - path: "components/cities/city-personas.tsx"
      provides: "Refactored city personas section with 6-card grid and shared component"
      contains: "PersonaCard"
    - path: "components/why/why-personas.tsx"
      provides: "Refactored why personas section using shared PersonaCard and custom icons"
      contains: "PersonaCard"
  key_links:
    - from: "components/cities/city-personas.tsx"
      to: "components/shared/persona-card.tsx"
      via: "import PersonaCard"
      pattern: "import.*PersonaCard.*from.*shared/persona-card"
    - from: "components/why/why-personas.tsx"
      to: "components/shared/persona-card.tsx"
      via: "import PersonaCard"
      pattern: "import.*PersonaCard.*from.*shared/persona-card"
    - from: "components/shared/persona-card.tsx"
      to: "components/icons/persona-icons.tsx"
      via: "icon map imports"
      pattern: "import.*from.*icons/persona-icons"
    - from: "lib/city-data.ts"
      to: "components/cities/city-personas.tsx"
      via: "CityData type with facilitator role"
      pattern: "facilitator"
---

<objective>
Update types, create custom persona icons, extract shared PersonaCard component, and rewire both persona sections (city-personas and why-personas) to render 6 cards in a responsive grid with Facilitator support.

Purpose: Foundation for Phase 13 city-specific copy -- the component must support 6 personas with the correct layout and card design before any copy can render.
Output: Updated CityData type, 6 custom SVG icons, shared PersonaCard, refactored city-personas.tsx and why-personas.tsx.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-persona-component-foundation/12-CONTEXT.md

<interfaces>
<!-- Existing CityData type that needs facilitator added to role union -->
From lib/city-data.ts:
```typescript
export type CityData = {
  slug: string;
  displayName: string;
  state: string;
  heroHeadline: string;
  heroSubhead: string;
  metaTagline: string;
  metaDescription: string;
  sceneIntro: string;
  sceneBlocks: [
    { heading: string; description: string; unsplashQuery: string },
    { heading: string; description: string; unsplashQuery: string },
    { heading: string; description: string; unsplashQuery: string },
  ];
  topPersonas: Array<{
    role: "chef" | "mixologist" | "venueHost" | "curator" | "guest";
    cityHeadline: string;
    cityDescription: string;
  }>;
  tier: 1 | 2 | 3;
};
```

<!-- Current city-personas.tsx imports and maps (to be replaced) -->
From components/cities/city-personas.tsx:
```typescript
import { ChefHat, Wine, Building2, Search, Users, ArrowRight } from "lucide-react";
const roleIconMap = { chef: ChefHat, mixologist: Wine, venueHost: Building2, curator: Search, guest: Users };
const roleDisplayMap = { chef: "Chef", mixologist: "Mixologist", venueHost: "Venue Host", curator: "Curator", guest: "Guest" };
```

<!-- Current why-personas.tsx persona data (to be refactored to use shared component) -->
From components/why/why-personas.tsx:
```typescript
const personas = [
  { id: "guest", icon: Users, title: "Guest", description: "..." },
  { id: "chef", icon: ChefHat, title: "Chef", description: "..." },
  { id: "mixologist", icon: Wine, title: "Mixologist", description: "..." },
  { id: "curator", icon: Compass, title: "Curator", description: "..." },
  { id: "venueHost", icon: Building2, title: "Venue Host", description: "..." },
  { id: "facilitator", icon: Network, title: "Facilitator", description: "..." },
];
```

<!-- City page renders CityPersonas with city prop -->
From app/cities/[slug]/page.tsx:
```typescript
<CityPersonas city={city} />
```

<!-- Why page renders WhyPersonas with no props -->
From app/why-tenseats/page.tsx:
```typescript
<WhyPersonas />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CityData type and create custom persona icon components</name>
  <files>lib/city-data.ts, components/icons/persona-icons.tsx</files>
  <action>
**lib/city-data.ts**: Add "facilitator" to the topPersonas role union type. Change:
```
role: "chef" | "mixologist" | "venueHost" | "curator" | "guest"
```
to:
```
role: "chef" | "mixologist" | "venueHost" | "curator" | "guest" | "facilitator"
```
Do NOT change any other part of CityData or any city data entries. The existing cities still have 5 personas -- Phase 13 will add the 6th.

**components/icons/persona-icons.tsx**: Create 6 custom monoline SVG React icon components. Each component accepts `className?: string` prop (defaulting to `"h-5 w-5"`). Use `currentColor` for stroke so they inherit text color from parent.

Design direction -- monoline (single stroke weight), clean line art:
- `GuestIcon`: Simple person silhouette with a plate/utensil suggestion -- conveys "diner at the table"
- `ChefIcon`: Chef's knife or toque (chef hat outline) -- conveys culinary craft
- `MixologistIcon`: Cocktail coupe glass or shaker -- conveys drink craft
- `CuratorIcon`: Compass or magnifying glass with a subtle food element -- conveys discovery
- `VenueHostIcon`: Door/archway or building facade -- conveys hosting/space
- `FacilitatorIcon`: Connected nodes or handshake -- conveys connecting people

All icons should use SVG `<path>` elements with `stroke="currentColor"`, `strokeWidth="1.5"`, `fill="none"`, viewBox `0 0 24 24`. Export each as a named export. Also export a `personaIconMap` object mapping role keys to icon components:
```typescript
export const personaIconMap = {
  guest: GuestIcon,
  chef: ChefIcon,
  mixologist: MixologistIcon,
  curator: CuratorIcon,
  venueHost: VenueHostIcon,
  facilitator: FacilitatorIcon,
} as const;
```
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CityData type accepts "facilitator" without type errors. 6 custom SVG icon components exported from persona-icons.tsx with personaIconMap.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared PersonaCard component</name>
  <files>components/shared/persona-card.tsx</files>
  <action>
Create a reusable PersonaCard component at `components/shared/persona-card.tsx`. This component is used by both city-personas and why-personas sections.

**Props interface:**
```typescript
interface PersonaCardProps {
  icon: React.ComponentType<{ className?: string }>;
  role: string;           // Display name e.g. "Chef", "Venue Host"
  headline?: string;      // City-specific headline (city-personas) or generic title (why-personas uses role as headline if not provided)
  description: string;    // City-specific or generic description
  index: number;          // For stagger animation delay
}
```

**Card design** (per user decision -- matches why-personas card style as baseline):
- Outer: `motion.div` with `rounded-2xl border border-border/50 p-6 bg-card/50 hover:border-border transition-colors`
- Framer Motion: `initial={{ opacity: 0, y: 24 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, margin: "-60px" }}`, transition duration 0.5 with `delay: index * 0.1`
- Icon container: `flex h-10 w-10 items-center justify-center rounded-xl bg-foreground/[0.06] dark:bg-foreground/[0.08] mb-4` -- render the icon component with `className="h-5 w-5 text-foreground/70"`
- Role badge: `text-xs uppercase tracking-widest text-muted-foreground font-medium mb-2` -- display below icon container
- Headline: `text-lg font-semibold mb-2` (only render if headline prop is provided)
- Description: `text-muted-foreground text-sm leading-relaxed`

Must be a "use client" component (uses framer-motion).

Export as named export: `export function PersonaCard(...)`.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>PersonaCard component exported with correct props interface, card design matching why-personas baseline style, framer-motion animation.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire city-personas and why-personas to use shared PersonaCard and custom icons</name>
  <files>components/cities/city-personas.tsx, components/why/why-personas.tsx</files>
  <action>
**components/cities/city-personas.tsx** -- Full refactor:
1. Remove ALL Lucide icon imports (ChefHat, Wine, Building2, Search, Users, ArrowRight)
2. Import `PersonaCard` from `@/components/shared/persona-card`
3. Import `personaIconMap` from `@/components/icons/persona-icons`
4. Import `Sparkles, ArrowRight` from `lucide-react` (for section CTA only)
5. Import `Button` from `@/components/ui/button` and `Link` from `next/link`
6. Remove `roleIconMap` and `roleDisplayMap` constants entirely
7. Create a local `roleDisplayMap` for display names:
   ```typescript
   const roleDisplayMap: Record<string, string> = {
     chef: "Chef", mixologist: "Mixologist", venueHost: "Venue Host",
     curator: "Curator", guest: "Guest", facilitator: "Facilitator",
   };
   ```
8. Update grid to: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
   (current is `grid-cols-1 sm:grid-cols-3` which is WRONG for the 2-col tablet breakpoint)
9. Replace inline card markup with `<PersonaCard>`:
   ```tsx
   {city.topPersonas.map((persona, index) => (
     <PersonaCard
       key={persona.role}
       icon={personaIconMap[persona.role]}
       role={roleDisplayMap[persona.role] || persona.role}
       headline={persona.cityHeadline}
       description={persona.cityDescription}
       index={index}
     />
   ))}
   ```
10. Remove per-card CTA buttons entirely (the individual "Join free as a..." links)
11. Add section-level CTA below the grid (matching why-personas CTA pattern):
    ```tsx
    <motion.div
      className="mt-16 flex justify-center"
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-60px" }}
      transition={{ duration: 0.5, delay: 0.4, ease: "easeOut" }}
    >
      <Link href={`/launch?city=${city.slug}`}>
        <Button
          variant="outline"
          size="lg"
          className="group rounded-full font-medium h-12 border-amber-500/30 text-amber-500 hover:bg-amber-500/10 hover:text-amber-400 hover:border-amber-500/50"
        >
          <Sparkles className="mr-2 h-4 w-4" />
          Find your seat
          <ArrowRight className="ml-2 h-4 w-4 transition-transform group-hover:translate-x-0.5" />
        </Button>
      </Link>
    </motion.div>
    ```
    Note: CTA links to `/launch?city=${city.slug}` (city-specific per user decision), NOT generic `/launch` or `/join`.

**components/why/why-personas.tsx** -- Refactor to use shared component:
1. Remove ALL Lucide icon imports (Users, ChefHat, Wine, Compass, Building2, Network)
2. Import `PersonaCard` from `@/components/shared/persona-card`
3. Import `personaIconMap` from `@/components/icons/persona-icons`
4. Keep `Sparkles, ArrowRight` imports for CTA
5. Update the `personas` array to reference `personaIconMap` instead of Lucide icons:
   ```typescript
   const personas = [
     { id: "guest", title: "Guest", description: "You bring curiosity..." },
     { id: "chef", title: "Chef", description: "You turn ingredients..." },
     { id: "mixologist", title: "Mixologist", description: "You pair flavors..." },
     { id: "curator", title: "Curator", description: "You find what others miss..." },
     { id: "venueHost", title: "Venue Host", description: "You create the space..." },
     { id: "facilitator", title: "Facilitator", description: "You connect the dots..." },
   ];
   ```
   (Remove the `icon` field from persona objects)
6. Replace inline card markup with `<PersonaCard>`:
   ```tsx
   {personas.map((persona, index) => (
     <PersonaCard
       key={persona.id}
       icon={personaIconMap[persona.id as keyof typeof personaIconMap]}
       role={persona.title}
       description={persona.description}
       index={index}
     />
   ))}
   ```
   Note: why-personas does NOT pass `headline` prop -- only role + description.
7. Keep the existing section header ("Who belongs here" + subtitle) and CTA ("Find your seat" linking to /join) exactly as they are.
8. Grid classes should remain: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` (already correct in current code).
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Both city-personas.tsx and why-personas.tsx use shared PersonaCard with custom SVG icons. City grid is 3x2/2x3/1x6 responsive. No per-card CTAs on city-personas -- single section CTA linking to /launch?city=[slug]. Why-personas keeps its existing CTA to /join. No Lucide persona icons remain in either file. TypeScript compiles clean. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero type errors
2. `npm run build` -- build succeeds with no errors
3. Visit any city page (e.g. /cities/chicago-il) -- 6-card grid renders (5 populated + 1 facilitator with placeholder if no data yet), responsive layout works at mobile/tablet/desktop breakpoints
4. Visit /why-tenseats -- 6 persona cards render with custom SVG icons (not Lucide), shared card design
5. Both sections use custom monoline SVG icons, not Lucide icons
6. City personas section has single "Find your seat" CTA below grid linking to /launch?city=[slug]
7. No per-card CTA buttons on city persona cards
</verification>

<success_criteria>
- CityData type includes "facilitator" in role union -- TypeScript accepts it
- 6 custom monoline SVG persona icons created and used in both sections
- Shared PersonaCard component used by both city-personas and why-personas
- City personas grid: 3x2 desktop / 2x3 tablet / 1x6 mobile (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)
- Card design: dark card with icon, uppercase role badge, headline, description
- Single section CTA on city-personas ("Find your seat" with amber styling, links to /launch?city=[slug])
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-persona-component-foundation/12-01-SUMMARY.md`
</output>
