---
phase: 22-verification-page
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - components/auth/verify-email-form.tsx
autonomous: false
requirements: [VERIFY-01]

must_haves:
  truths:
    - "After OTP verification, guest users are routed to /launch/success/guest"
    - "After OTP verification, vendor users (chef, mixologist, venueHost, creator) are routed to /launch/success/vendor"
    - "The full flow — signup to verify page to OTP entry to success stub — works end to end"
  artifacts:
    - path: "components/auth/verify-email-form.tsx"
      provides: "Role-based post-verification routing"
      contains: "launch/success/guest"
  key_links:
    - from: "components/auth/verify-email-form.tsx"
      to: "userRoles table"
      via: "Convex query to determine user role after verification"
      pattern: "userRoles|launch/success/(guest|vendor)"
---

<objective>
Wire post-OTP-verification routing so verified users land on the correct success page based on their role, then visually verify the entire flow from signup through verification page.

Purpose: Complete the VERIFY-01 flow end-to-end — signup redirects to branded verify page, OTP entry routes to role-appropriate success page.
Output: Role-based routing in verify-email-form.tsx, visual confirmation of the full flow.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-page/22-CONTEXT.md
@.planning/phases/22-verification-page/22-01-SUMMARY.md

@components/auth/verify-email-form.tsx

<interfaces>
<!-- Key integration points -->

From components/auth/verify-email-form.tsx (line 177-180):
```typescript
// Current post-verification redirect — must change to role-based routing
setVerified(true);
setTimeout(() => {
  router.push("/");
}, 1500);
```

From CONTEXT.md decisions:
- Routing logic: non-guest role (chef, mixologist, creator, venueHost) -> vendor success
- guest role -> guest success
- userRoles table is the canonical source for role determination

Convex pattern (from existing codebase):
```typescript
import { useQuery } from "convex/react";
import { api } from "@/convex/_generated/api";
// Query userRoles to determine if user has vendor role
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire role-based post-verification routing</name>
  <files>components/auth/verify-email-form.tsx</files>
  <action>
Modify the success handler in `verify-email-form.tsx` (around line 177-180) to route based on the user's role after OTP verification succeeds:

1. After `setVerified(true)`, query the userRoles table to determine the user's role. The user is now authenticated (OTP just verified their email), so Convex queries should work.

2. Routing logic:
   - If user has any vendor role (chef, mixologist, venueHost, creator) -> `router.push("/launch/success/vendor")`
   - If user has guest role or no specific vendor role -> `router.push("/launch/success/guest")`
   - Fallback (query fails or no role found): default to `/launch/success/guest`

3. Implementation approach: Since this is inside a callback (submitOtp), the simplest pattern is to fetch the role inline after verification succeeds. Use the Convex client to query userRoles for the current user. Check the existing codebase patterns for how Convex queries are called — look at how `useQuery` or `convexClient` is used elsewhere.

4. If a direct Convex query inside the callback is not feasible (hook rules), an alternative: add a `useQuery` for userRoles at the component level, and read its value inside the success handler. The query may initially return undefined while loading — in that case, add a brief poll or default to guest.

5. Keep the 1500ms delay for the success animation before redirect. The role query should resolve within that window since the user is already authenticated.

Important: Do NOT change any other behavior in this file — only modify the post-verification redirect destination. The OTP input, resend, cooldown, expiry, shake animation, and all other logic remain exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | tail -5 && grep -c "launch/success" components/auth/verify-email-form.tsx</automated>
  </verify>
  <done>After successful OTP verification, guest users route to /launch/success/guest and vendor users route to /launch/success/vendor. The 1500ms success animation plays before redirect. All other verify-email-form behavior is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of full signup-to-verify flow</name>
  <files>none</files>
  <action>
Human verifies the complete flow built across Plans 01 and 02. No code changes — visual and functional verification only.

What was built:
- PostSignupLayout shared wrapper with dark premium design and entrance animations
- VerticalTimeline reusable component with step dots and connecting lines
- Branded /launch/verify page with green check pulse, oversized headline, timeline, CTA, and resend
- Queue signup form redirected to /launch/verify
- Role-based post-OTP routing to /launch/success/guest or /launch/success/vendor
- Stub routes for guest and vendor success pages
  </action>
  <verify>
    1. Run `npm run dev` and open http://localhost:3000/launch
    2. Fill in the queue signup form with a test email and submit
    3. Confirm you are redirected to /launch/verify (not /verify-email)
    4. On /launch/verify, check:
       - LandingHeader appears at top
       - Dark premium background, centered content
       - Green check circle with subtle pulse/glow animation
       - Oversized headline with brand-playful tone
       - Subline showing masked email address
       - 3-step vertical timeline (Check email -> Enter code -> You're in)
       - "Enter Verification Code" CTA button
       - "Didn't get it?" resend link
       - Entrance animation (fade + slide up) on page load
    5. Click "Enter Verification Code" — should navigate to /verify-email with existing OTP form
    6. Visit /launch/verify directly without signing up — should redirect to /launch
    7. Visit /launch/success/guest — should show stub placeholder
    8. Visit /launch/success/vendor — should show stub placeholder
    9. Overall: does the page feel premium and warm, not generic?
  </verify>
  <done>User has approved the verification page design and full flow, or described issues for fixing.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors
2. `verify-email-form.tsx` contains role-based routing to `/launch/success/guest` or `/launch/success/vendor`
3. Full flow works: signup -> /launch/verify -> OTP entry -> success page
4. Visual checkpoint confirms branded design quality
</verification>

<success_criteria>
- Post-OTP routing sends guest users to /launch/success/guest and vendor users to /launch/success/vendor
- The full flow from queue signup through verification page to success stub is visually confirmed
- The verification page design is approved as premium and on-brand
</success_criteria>

<output>
After completion, create `.planning/phases/22-verification-page/22-02-SUMMARY.md`
</output>
