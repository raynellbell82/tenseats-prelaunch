---
phase: 22-verification-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/post-signup/post-signup-layout.tsx
  - components/post-signup/vertical-timeline.tsx
  - app/launch/verify/page.tsx
  - app/launch/success/guest/page.tsx
  - app/launch/success/vendor/page.tsx
  - components/launch/launch-queue-signup-form.tsx
autonomous: true
requirements: [VERIFY-01, VERIFY-02, DESIGN-01]

must_haves:
  truths:
    - "After queue signup, user lands on /launch/verify with branded 'check your email' messaging"
    - "The verification page works identically for guest and vendor signups (no role-specific content)"
    - "The page uses a distinctive dark premium design — not a generic confirmation screen"
    - "A PostSignupLayout wrapper exists that all 3 post-signup pages will share"
    - "A VerticalTimeline component renders step-by-step visual flow with dots and connecting lines"
    - "Stub routes exist at /launch/success/guest and /launch/success/vendor"
  artifacts:
    - path: "components/post-signup/post-signup-layout.tsx"
      provides: "Shared layout wrapper with LandingHeader, dark background, entrance animation"
      exports: ["PostSignupLayout"]
    - path: "components/post-signup/vertical-timeline.tsx"
      provides: "Reusable vertical timeline with step data as props"
      exports: ["VerticalTimeline"]
    - path: "app/launch/verify/page.tsx"
      provides: "Branded verification page at /launch/verify"
      min_lines: 40
    - path: "app/launch/success/guest/page.tsx"
      provides: "Stub placeholder for Phase 23"
    - path: "app/launch/success/vendor/page.tsx"
      provides: "Stub placeholder for Phase 24"
  key_links:
    - from: "components/launch/launch-queue-signup-form.tsx"
      to: "/launch/verify"
      via: "router.push redirect after signup"
      pattern: "router\\.push.*launch/verify"
    - from: "app/launch/verify/page.tsx"
      to: "components/post-signup/post-signup-layout.tsx"
      via: "import PostSignupLayout"
      pattern: "import.*PostSignupLayout"
    - from: "app/launch/verify/page.tsx"
      to: "components/post-signup/vertical-timeline.tsx"
      via: "import VerticalTimeline"
      pattern: "import.*VerticalTimeline"
---

<objective>
Build the shared post-signup design system and the branded "almost there" verification page at /launch/verify. This establishes the design language (dark premium aesthetic, oversized typography, entrance animations) that Phases 23 and 24 will extend. Also wires the queue signup form to redirect here and creates stub routes for future success pages.

Purpose: VERIFY-01, VERIFY-02, and DESIGN-01 — users land on a warm, branded page after signup telling them to check their email, and the reusable design kit is ready for the remaining post-signup pages.
Output: PostSignupLayout + VerticalTimeline shared components, /launch/verify page, signup redirect wired, stub routes created.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-page/22-CONTEXT.md

@components/landing/landing-header.tsx
@components/launch/launch-queue-signup-form.tsx
@app/launch/success/page.tsx
@components/icons/social-icons.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From components/landing/landing-header.tsx:
```typescript
export function LandingHeader(): JSX.Element;
```

From components/launch/launch-queue-signup-form.tsx (line 125):
```typescript
// Current redirect after signup — must change to /launch/verify
router.push("/verify-email");
```

From components/icons/social-icons.tsx:
```typescript
export function InstagramIcon(props: IconProps): JSX.Element;
export function PinterestIcon(props: IconProps): JSX.Element;
```

Established patterns:
- Tailwind v4 with `@import "tailwindcss"` syntax
- Framer Motion for animations (motion.div, whileInView, staggered entrance)
- LandingHeader at top of every page
- `authClient` from `lib/auth-client` for auth operations
- `sessionStorage.getItem("otp-verify-email")` for email persistence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared post-signup design system components</name>
  <files>components/post-signup/post-signup-layout.tsx, components/post-signup/vertical-timeline.tsx</files>
  <action>
Create `components/post-signup/` directory with two shared components:

**PostSignupLayout** (`post-signup-layout.tsx`):
- "use client" component wrapping all post-signup pages
- Renders LandingHeader at top (import from `@/components/landing/landing-header`)
- Dark background matching existing site dark theme (not a new color — use the site's existing bg)
- Centers content vertically and horizontally with generous padding (py-20+ on desktop, py-12 on mobile)
- max-w-2xl container, centered
- Framer Motion entrance animation: fade + slide up (opacity 0->1, y 20->0) with ~0.6s duration, ease-out
- Accepts `children` prop
- Stagger children animation using Framer Motion's staggerChildren (0.15s delay between items)

**VerticalTimeline** (`vertical-timeline.tsx`):
- Accepts array of step objects: `{ label: string; detail?: string; active?: boolean }`
- Renders a vertical timeline with dots connected by lines
- Each step: small circle dot (8-10px) on the left, label text to the right, optional detail text below in muted color
- Connecting line between dots (thin line, muted color)
- Active step dot uses green-500 fill, inactive dots use border-only with muted stroke
- All steps visible at once (not a progress indicator — it's a visual list)
- Clean, minimal styling with Tailwind — no heavy borders or cards
- Responsive: works well on mobile with appropriate text sizing

Both components use Tailwind for styling and Framer Motion for any animations.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit --strict components/post-signup/post-signup-layout.tsx components/post-signup/vertical-timeline.tsx 2>&1 | head -20</automated>
  </verify>
  <done>PostSignupLayout renders a dark, centered container with LandingHeader and entrance animations. VerticalTimeline renders a vertical step list with dots and connecting lines. Both compile without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification page, wire signup redirect, create stub routes</name>
  <files>app/launch/verify/page.tsx, app/launch/success/guest/page.tsx, app/launch/success/vendor/page.tsx, components/launch/launch-queue-signup-form.tsx</files>
  <action>
**Verification page** (`app/launch/verify/page.tsx`):
- "use client" page component at route `/launch/verify`
- Wrapped in PostSignupLayout (import from `@/components/post-signup/post-signup-layout`)
- Guard: if no `sessionStorage.getItem("otp-verify-email")`, redirect to `/launch` (user arrived without signing up)
- Retrieve the user's email from sessionStorage for display (masked: `u***@example.com`)

Content structure (top to bottom):
1. **Green check circle**: A div with rounded-full, bg-green-500/20, containing a CheckCircle2 icon (lucide-react) in green-500. Apply a gentle CSS pulse animation (scale 1->1.05->1 + opacity glow) that loops infinitely. Size: h-20 w-20 outer circle, h-10 w-10 icon.
2. **Headline**: Oversized (text-4xl md:text-5xl font-bold tracking-tight). Use the tenseats-copy-writer skill to generate the headline — the brief: post-signup verification page, table metaphor encouraged (e.g., "The Table's Almost Set"), brand-playful tone. Write 3-4 options inline as comments, pick the strongest one. The headline must follow brand voice rules from the skill references.
3. **Subline**: 1-2 sentences in text-lg text-muted-foreground, telling user to check their email for the verification code. Mention the masked email. Example direction: "We sent a 6-digit code to u***@example.com. Enter it on the next screen to confirm your seat."
4. **VerticalTimeline**: 3 steps — (1) "Check your email" with detail "Look for a 6-digit code", (2) "Enter your code" with detail "On the verification screen", (3) "You're in" with detail "Welcome to Tenseats". First step marked active.
5. **CTA button**: Links to `/verify-email` — "Enter Verification Code" styled as primary button (bg-foreground text-background rounded-full px-8 py-3 font-semibold). This takes them to the existing OTP entry form.
6. **Resend link**: Below button, smaller text: "Didn't get it?" with a button/link that calls `authClient.emailOtp.sendVerificationOtp` to resend. Include cooldown logic (60s) similar to verify-email-form.tsx.

All text centered. Large spacing between sections (space-y-8 or similar). The overall feel should be premium and warm — like an exclusive invite confirmation, not a generic form success.

**Signup redirect change** (`components/launch/launch-queue-signup-form.tsx`):
- Change line 125 from `router.push("/verify-email")` to `router.push("/launch/verify")`
- No other changes to this file

**Stub routes**:
- `app/launch/success/guest/page.tsx`: Simple server component with PostSignupLayout wrapping a centered message: "Guest success page coming soon" with a Link back to `/`. This is a placeholder for Phase 23.
- `app/launch/success/vendor/page.tsx`: Same pattern: "Vendor success page coming soon" with Link back to `/`. Placeholder for Phase 24.

Note: The existing `app/launch/success/page.tsx` remains untouched — it will coexist until Phases 23/24 replace it.
  </action>
  <verify>
    <automated>cd /Users/tenseats/Documents/dev/tenseats-prelaunch && npx tsc --noEmit 2>&1 | tail -5 && echo "---" && test -f app/launch/verify/page.tsx && echo "verify page exists" && test -f app/launch/success/guest/page.tsx && echo "guest stub exists" && test -f app/launch/success/vendor/page.tsx && echo "vendor stub exists" && grep -c "launch/verify" components/launch/launch-queue-signup-form.tsx</automated>
  </verify>
  <done>The /launch/verify page renders with green check pulse, oversized headline, masked email subline, 3-step vertical timeline, CTA to /verify-email, and resend link. Queue signup form redirects to /launch/verify. Stub routes exist for guest and vendor success pages. Full project compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no type errors across the project
2. `app/launch/verify/page.tsx` exists and imports PostSignupLayout + VerticalTimeline
3. `components/post-signup/post-signup-layout.tsx` exports PostSignupLayout with LandingHeader + entrance animation
4. `components/post-signup/vertical-timeline.tsx` exports VerticalTimeline accepting step array props
5. `components/launch/launch-queue-signup-form.tsx` redirects to `/launch/verify` (not `/verify-email`)
6. Stub routes exist at `app/launch/success/guest/page.tsx` and `app/launch/success/vendor/page.tsx`
</verification>

<success_criteria>
- After queue signup, user is redirected to /launch/verify (not /verify-email)
- The verification page shows branded dark design with green check pulse, oversized headline, timeline, and CTA
- The page is role-agnostic — same content for guest and vendor signups
- PostSignupLayout and VerticalTimeline are reusable for Phases 23/24
- Stub routes exist for /launch/success/guest and /launch/success/vendor
- Full project type-checks successfully
</success_criteria>

<output>
After completion, create `.planning/phases/22-verification-page/22-01-SUMMARY.md`
</output>
