---
phase: 21-social-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/icons/social-icons.tsx
autonomous: false
requirements:
  - SOCIAL-01
  - SOCIAL-02

must_haves:
  truths:
    - "An Instagram icon component renders at standard sizes (h-5 w-5, h-6 w-6, h-8 w-8) with correct proportions"
    - "A Pinterest icon component renders at standard sizes with correct proportions"
    - "Both icons inherit color from parent via currentColor, working in both light and dark themes"
    - "Both icons follow the same monoline stroke style as the existing persona icons in persona-icons.tsx"
  artifacts:
    - path: "components/icons/social-icons.tsx"
      provides: "InstagramIcon and PinterestIcon React components"
      exports: ["InstagramIcon", "PinterestIcon"]
  key_links:
    - from: "components/icons/social-icons.tsx"
      to: "components/icons/persona-icons.tsx"
      via: "Same IconProps interface pattern, identical SVG attributes"
      pattern: "stroke=\"currentColor\".*strokeWidth=\"1.5\""
---

<objective>
Create custom Instagram and Pinterest SVG icon components matching the project's existing monoline icon style.

Purpose: These social icons are needed across success pages (Phases 23 and 24) to link users to Tenseats social channels. They must match the brand's custom icon aesthetic rather than using generic Lucide icons.

Output: A single `social-icons.tsx` file exporting `InstagramIcon` and `PinterestIcon` React components.
</objective>

<execution_context>
@/Users/tenseats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tenseats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/icons/persona-icons.tsx

<interfaces>
<!-- Existing icon pattern from persona-icons.tsx. New social icons MUST follow this exact pattern. -->

From components/icons/persona-icons.tsx:
```typescript
interface IconProps {
  className?: string;
}

// Every icon follows this exact SVG structure:
// - xmlns="http://www.w3.org/2000/svg"
// - viewBox="0 0 24 24"
// - fill="none"
// - stroke="currentColor"
// - strokeWidth="1.5"
// - strokeLinecap="round"
// - strokeLinejoin="round"
// - className defaults to "h-5 w-5"
// - Monoline style: single-weight strokes, no fills, clean geometric shapes

export function GuestIcon({ className = "h-5 w-5" }: IconProps) { ... }
export function ChefIcon({ className = "h-5 w-5" }: IconProps) { ... }
// ... 6 icons total, all with same structure
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Instagram and Pinterest monoline SVG icon components</name>
  <files>components/icons/social-icons.tsx</files>
  <action>
Create `components/icons/social-icons.tsx` with two exported components: `InstagramIcon` and `PinterestIcon`.

Both MUST use the identical SVG wrapper pattern from `persona-icons.tsx`:
- Same `IconProps` interface with `className?: string` defaulting to `"h-5 w-5"`
- Same SVG attributes: `xmlns`, `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth="1.5"`, `strokeLinecap="round"`, `strokeLinejoin="round"`

**InstagramIcon design (monoline, 24x24 viewBox):**
- Rounded rectangle body (the camera shape): use a `rect` with rx for rounded corners, approximately x=2 y=2 width=20 height=20 rx=5
- Circle lens in center: `circle` at cx=12 cy=12 r=5
- Small dot for flash: `circle` at approximately cx=17.5 cy=6.5 r=1.5

**PinterestIcon design (monoline, 24x24 viewBox):**
- The Pinterest "P" pin shape rendered as strokes, not fills
- Use a circular boundary (circle cx=12 cy=12 r=10) as the outer ring
- Inside, draw a stylized "P" using path strokes: a vertical line with a curved top-right bump (the pin shape)
- The pin tail curves down-left from the bottom of the P

Both icons must look recognizable at h-5 (20px) and scale cleanly to h-8 (32px). Keep paths simple -- fewer elements render more crisply at small sizes.

Add a JSDoc comment block at the top of the file matching the style in persona-icons.tsx:
```
/**
 * Custom monoline SVG icon components for social media links.
 * Each icon uses stroke="currentColor" so it inherits text color from parent.
 * strokeWidth="1.5", fill="none", viewBox="0 0 24 24"
 */
```
  </action>
  <verify>
    <automated>npx tsc --noEmit components/icons/social-icons.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
- social-icons.tsx exports InstagramIcon and PinterestIcon
- Both components accept optional className prop
- Both use identical SVG attributes as persona-icons.tsx (currentColor, strokeWidth 1.5, viewBox 0 0 24 24, fill none)
- TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of social icon rendering</name>
  <files>components/icons/social-icons.tsx</files>
  <action>
Human verifies the rendered Instagram and Pinterest icons match the brand's monoline aesthetic.
  </action>
  <verify>
    <automated>echo "Checkpoint: manual visual verification required"</automated>
  </verify>
  <done>
User confirms both icons are visually recognizable and match monoline style.

Verification steps:
1. Open the dev server: `npm run dev` (port 3001)
2. The executor will create a temporary test page or use browser dev tools to render the icons
3. Verify at the component level:
   - Both icons render as recognizable Instagram and Pinterest logos
   - Icons display correctly at small (20px), medium (24px), and large (32px) sizes
   - Icons inherit text color (try both light and dark theme via theme toggle)
   - Stroke style matches the monoline aesthetic of the persona icons (same weight, no fills)
4. If icons look off, describe what needs adjustment

Resume signal: Type "approved" or describe visual issues to fix
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Both icon components export correctly from social-icons.tsx
- Icons visually match the monoline stroke style of persona-icons.tsx
- Icons render correctly in both light and dark themes via currentColor
</verification>

<success_criteria>
- InstagramIcon and PinterestIcon are importable from `components/icons/social-icons.tsx`
- Both use `currentColor` stroke, `strokeWidth="1.5"`, `fill="none"`, `viewBox="0 0 24 24"`
- Both are visually recognizable at standard icon sizes (20-32px)
- Both match the monoline aesthetic of existing persona icons
</success_criteria>

<output>
After completion, create `.planning/phases/21-social-icons/21-01-SUMMARY.md`
</output>
